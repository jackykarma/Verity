# 需求质量清单：FEAT-009 SDK 事件回调系统

**Feature**：FEAT-009 - SDK 事件回调系统
**关联 Spec**：`specs/epics/EPIC-001-media-viewer-sdk/features/FEAT-009-event-callbacks/spec.md`
**检查日期**：2026-03-22
**检查人**：（待填写）
**状态**：待检查

---

## 一、完整性检查

| # | 检查项 | 状态 | 备注 |
|---|--------|------|------|
| C-01 | 所有 8 个回调事件均已定义对应 FR（FR-001～FR-008） | 通过 | 每个回调一条 FR |
| C-02 | 每条 FR 均有对应 AC 引用 | 通过 | AC-074～AC-083 覆盖全部 FR |
| C-03 | NFR 覆盖性能、功耗、内存、安全隐私、可观测性、可靠性六个维度 | 通过 | 共 11 条 NFR |
| C-04 | 核心用户旅程已定义（图片防黑屏、视频防黑屏各 1 个） | 通过 | 旅程 1 + 旅程 2 |
| C-05 | 边界与异常场景已覆盖：不注册回调、高频回调、回调内异常 | 通过 | 共 6 个边界场景 |
| C-06 | 核心实体已定义（`MediaViewerListener`、`VideoPlaybackState`、`MediaLoadError`） | 通过 | 3 个实体 |
| C-07 | 依赖关系已明确（FEAT-001、FEAT-002、FEAT-003） | 通过 | 含故障模式描述 |
| C-08 | 假设与约束已填写，含假设不成立的降级处理 | 通过 | 3 条假设、4 条约束 |
| C-09 | 需求追溯表已预留占位行（全部 FR/NFR） | 通过 | 21 行占位 |
| C-10 | 变更记录初始条目已填写 | 通过 | v0.1.0 初稿条目 |

---

## 二、可测试性检查

| # | 检查项 | 状态 | 备注 |
|---|--------|------|------|
| T-01 | 每条 FR 描述为「系统必须…」��式，可验证 | 通过 | FR-001～FR-010 均含可验证动词 |
| T-02 | AC-074 `onPageChanged` from/to 值可通过仪器测试精确验证 | 通过 | 误差为 0 |
| T-03 | AC-075 `onThumbnailLoaded` 触发时机可通过 UI Automator 截图序列验证 | 通过 | 含预加载场景 |
| T-04 | AC-076 高清图回调前不显示高清图，可通过 UI Automator 截图验证 | 通过 | 明确验证方式 |
| T-05 | AC-077 防黑屏可通过自动化截帧测试验证，无黑帧判定标准明确 | 通过 | 截帧序列方法 |
| T-06 | AC-078 ���频 4 种状态回调可通过模拟播放器状态机的集成测试验证 | 通过 | 逐状态列举 |
| T-07 | AC-079 `onVideoVolumeChanged` 参数范围 [0.0,1.0] 可自动化验证 | 通过 | 范围约束明确 |
| T-08 | AC-080 null 监听器场景可通过单元测试 100% 覆盖所有回调发布路径 | 通过 | NFR-REL-001 有对应单测要求 |
| T-09 | AC-081 `onPageScrolled` 耗时 p99 ≤ 1 ms 可通过 Perfetto 采集验证 | 通过 | 含采集工具说明 |
| T-10 | AC-082 内存泄漏可通过 LeakCanary + 旋转屏幕验证 | 通过 | 明确操作步骤（旋转 3 次） |
| T-11 | AC-083 回调异常隔离可通过集成测试模拟异常场景验证 | 通过 | 含日志验证要求 |
| T-12 | NFR-POWER-001 无后台轮询可通过 Battery Historian 验证 | 通过 | 验收方式明确 |
| T-13 | NFR-MEM-002 DEX 增量 ≤ 20 KB 可通过 APK Analyzer 验证 | 通过 | 工具明确 |

---

## 三、量化性检查（NFR）

| # | NFR ID | 量化指标 | 验收方式 | 状态 |
|---|--------|----------|----------|------|
| Q-01 | NFR-PERF-001 | `onPageScrolled` 分发耗时 p99 ≤ 1 ms | Perfetto / Systrace | 通过 |
| Q-02 | NFR-PERF-002 | `onPageChanged` 触发延迟 ≤ 16 ms（1 帧） | Choreographer 帧时间戳对比 | 通过 |
| Q-03 | NFR-POWER-001 | 无后台 WakeLock / 定��器 | Battery Historian 无异常唤醒增量 | 通过 |
| Q-04 | NFR-MEM-001 | Activity 旋转 3 次后无 Listener 泄漏 | LeakCanary | 通过 |
| Q-05 | NFR-MEM-002 | 接口 DEX 增量 ≤ 20 KB | APK Analyzer | 通过 |
| Q-06 | NFR-SEC-001 | 回调参数不含 PII（URL 路径、用户标识） | 代码审查 | 通过 |
| Q-07 | NFR-OBS-001 | Debug 日志格式规范，Release 可配置关闭 | 代码审查 + 运行验证 | 通过 |
| Q-08 | NFR-OBS-002 | 回调内异常以 Warning 日志记录含堆栈 | 集成测试日志捕获 | 通过 |
| Q-09 | NFR-REL-001 | null 监听器场景 100% 不崩溃 | 单元测试全路径覆盖 | 通过 |
| Q-10 | NFR-REL-002 | 回调异常不影响后续回调 | 集成测试（异常模拟） | 通过 |
| Q-11 | NFR-REL-003 | 首帧未触发时封面图持续显示 | 自动化截帧验证 | 通过 |

---

## 四、边界场景覆盖检查

| # | 边界场景 | 是否已在 Spec 中描述 | 对应 FR/NFR/AC |
|---|----------|----------------------|----------------|
| B-01 | 接入方不注册任何回调（null 监听器） | 是 | FR-009、NFR-REL-001、AC-080 |
| B-02 | 快速连续翻页导致 `onPageScrolled` 高频触发（60~120 次/秒） | 是 | NFR-PERF-001、AC-081 |
| B-03 | 接入方在回调内抛出未捕获 RuntimeException | 是 | NFR-REL-002、NFR-OBS-002、AC-083 |
| B-04 | 回调接口持有 Activity 引用导致潜在内存泄漏 | 是 | NFR-MEM-001、AC-082 |
| B-05 | 视频解码失败，`onVideoFirstFrameReady` 永不触发 | 是 | FR-008、NFR-REL-003 |
| B-06 | 非当前页预加载完成触发 `onThumbnailLoaded` | 是 | FR-003、AC-075 |
| B-07 | 网络抖动导致同一 position 多次重加载，回调多次触发 | 是（边界场景第 6 条） | FR-002、FR-003 |

---

## 五、范围清晰度检查

| # | 检查项 | 状态 | 备注 |
|---|--------|------|------|
| S-01 | Out of Scope 明确排除各回调事件的触发逻辑（由 FEAT-001/002/003 实现） | 通过 | 防止范围漂移至其他 Feature |
| S-02 | Out of Scope 明确排除回调数据的持久化和埋点上报 | 通过 | 减少实现歧义 |
| S-03 | Out of Scope 明确排除接入方 UI 实现（Indicator 等） | 通过 | 边界清晰 |
| S-04 | In Scope 明确包含接口定义、发布机制、线程调度约束 | 通过 | 本 Feature 核心职责 |
| S-05 | In Scope 明确包含防黑屏回调时序约束（图片 + 视频） | 通过 | 关键业务规则 |

---

## 六、依赖完整性检查

| # | 检查项 | 状态 | 备注 |
|---|--------|------|------|
| D-01 | 上游依赖 FEAT-002 已描述接入契约（翻页事件触发时机） | 通过 | 含故障降级描述 |
| D-02 | 上游依赖 FEAT-003 已描述接入契约（视频状态事件触发时机） | 通过 | 含故障降级描述 |
| D-03 | 上游依赖 FEAT-001 已描述接入契约（图片加载事件触发时机） | 通过 | 已补充说明 |
| D-04 | 下游影响已描述（接入方 UI 联动、FEAT-010 Demo App） | 通过 | 明确受影响范围 |
| D-05 | FEAT-003 底层播放器不支持首帧回调的假设已列出，含降级说明 | 通过 | 假设条目第 2 条 |

---

## 七、待澄清问题

> 以下问题在转入 Plan 阶段前建议与相关 Feature 负责人确认。

| # | 问题 | 影响范围 | 优先级 | 状态 |
|---|------|----------|--------|------|
| Q-001 | FEAT-002 翻页器能否在每帧提供精确 positionOffset（[0.0, 1.0)）？若底层使用 ViewPager2，offset 计算逻辑需对齐 | FR-002、AC-074 | 高 | 待确认 |
| Q-002 | FEAT-003 底层视频播放器（ExoPlayer / MediaPlayer）是否有明确的「首帧解码完成」回调？若使用 ExoPlayer，确认 `onRenderedFirstFrame` 可用性 | FR-005、AC-077 | 高 | 待确认 |
| Q-003 | `MediaViewerListener` 是否需要支持多监听器注册（观察者列表）？当前 Spec 未明确是单一 Listener 还是可添加多个 | FR-009 | 中 | 待澄清 |
| Q-004 | `onLoadError` 中的 `MediaLoadError` 是否需要区分图片错误与视频错误（独立枚举 vs 统一枚举+类型字段）？ | FR-008 | 中 | 待澄清 |
| Q-005 | `onPageScrolled` 是否需要在 SDK 侧做节流（Throttle）以降低接入方回调压力？当前 Spec 约束接入方自行保证轻量，但部分接入方可能无法满足 | NFR-PERF-001 | 低 | 待讨论 |

---

## 八、整体评估

| 维度 | 评分（1-5） | 说明 |
|------|------------|------|
| 完整性 | 5 | 8 个回调全部覆盖，6 个 NFR 维度齐全 |
| 可测试性 | 5 | 所有 AC 含具体验证方式和工具 |
| 量化性 | 5 | 所有 NFR 含数值指标和验收工具 |
| 边界覆盖 | 5 | 7 个边界场景，含高频、异常、泄漏 |
| 范围清晰度 | 5 | In/Out Scope 均明确，无歧义 |
| 依赖完整性 | 5 | 3 个上游依赖均含故障模式 |

**总体结论**：规格质量达到「就绪（可进入 Plan）」标准，建议确认上述 5 个待澄清问题后执行 `/aisdd.gate spec-ready`。
