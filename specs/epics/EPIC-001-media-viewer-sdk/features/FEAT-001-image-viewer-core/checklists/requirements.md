# 需求质量清单：FEAT-001 图片浏览核心能力

**Feature ID**：FEAT-001
**Feature Version**：v0.1.0
**生成时间**：2026-03-22
**对应 spec**：`specs/epics/EPIC-001-media-viewer-sdk/features/FEAT-001-image-viewer-core/spec.md`

---

## 一、FR 质量检查

> 规则：每条 FR 须满足"可测试"（有明确输入/输出）且有 AC 覆盖。

| FR ID | FR 描述摘要 | 是否可测试 | AC 覆盖 | 备注 |
|---|---|---|---|---|
| FR-001 | JPEG 解码并渲染 | 是 | AC-001 | |
| FR-002 | PNG（含透明通道）解码并渲染 | 是 | AC-002 | |
| FR-003 | HEIF/HEIC 解码（API 28+），低版本降级 | 是 | AC-003 | 需明确低版本降级策略（error placeholder vs 软解）；已在 FR-003 中描述 |
| FR-004 | 静态 / 动态 WebP 解码并渲染 | 是 | AC-004 | |
| FR-005 | BMP 解码并渲染 | 是 | AC-005 | |
| FR-006 | GIF 按帧率播放，离开视口停止 | 是 | AC-006 | |
| FR-007 | 超大图分块加载，仅解码视口内 Tile | 是 | AC-007、AC-009 | |
| FR-008 | 放大后 Tile 重解码，不允许拉伸模糊 | 是 | AC-008、AC-013 | |
| FR-009 | 双击放大 2 倍，到 5× 上限后不响应 | 是 | AC-010 | |
| FR-010 | 双指捏合缩放 [1×, 5×]，超限有阻尼回弹 | 是 | AC-011、AC-012 | |
| FR-011 | 单指平移（内容超出视口时），边缘阻尼回弹 | 是 | AC-036、AC-037 | |
| FR-012 | 双指旋转，松手对齐 90° 倍数，有动画 | 是 | AC-038、AC-071 | |
| FR-013 | 加载中展示 loading 状态（接入方定义 UI） | 是 | — | **待补充 AC**：需要一条明确 loading 状态触发/消失的 AC |
| FR-014 | 加载/解码失败展示 error placeholder（接入方定义） | 是 | AC-003（部分）| **建议补充独立 AC**：覆盖 JPEG/PNG 损坏等失败场景 |
| FR-015 | onLowMemory 时自动释放 Tile 缓存，降级解码精度，不崩溃 | 是 | — | **待补充 AC**：需引用 NFR-MEM-003 的量化指标 |
| FR-016 | Tile 大小/内存上限/解码线程数可配置，有合理默认值 | 是 | — | **建议补充 AC**：验证默认配置下可正常工作，且自定义配置生效 |

**FR 质量总结**：
- FR-001 ~ FR-012 均有可测试描述及对应 AC，质量达标。
- FR-013、FR-015、FR-016 缺少独立 AC，建议在下一个 spec 修订版本中补充。
- FR-014 建议补充针对损坏文件的独立 AC（当前 AC-003 仅覆盖版本不兼容场景）。

---

## 二、NFR 质量检查

> 规则：每条 NFR 须量化（有具体数值/阈值）且有验收方式。

### 性能（Performance）

| NFR ID | 指标摘要 | 是否量化 | 验收方式 | 备注 |
|---|---|---|---|---|
| NFR-PERF-001 | 手势操作帧率 ≥ 60fps（120fps 设备 ≥ 120fps） | 是（帧率目标明确） | Perfetto 录制 10 秒，丢帧率 ≤ 1% | 达标 |
| NFR-PERF-002 | 普通图片首帧可见 p95 ≤ 500ms（中端设备） | 是 | Benchmark 测试库，100 次采样 | 达标；建议明确"中端设备"的参考机型 |
| NFR-PERF-003 | 超大图首帧可交互 p95 ≤ 2000ms（中端设备） | 是 | Benchmark 测试库，20 次采样 | 达标；采样数偏少，建议至少 50 次 |
| NFR-PERF-004 | Tile 重解码完成 p95 ≤ 800ms（中端设备） | 是 | Benchmark 测试库，50 次采样 | 达标 |

### 功耗（Power）

| NFR ID | 指标摘要 | 是否量化 | 验收方式 | 备注 |
|---|---|---|---|---|
| NFR-POWER-001 | 静止查看时不执行后台解码，CPU 趋近 0% | 是（行为约束量化） | Battery Historian / Energy Profiler，60 秒测试 | 达标 |
| NFR-POWER-002 | GIF 播放时解码在非主线程，不引入额外 CPU 开销 | 是（线程约束明确） | Profiler 线程分布观察 | 达标；建议补充 GIF 播放时 CPU% 上限（如 ≤ 5% on 中端设备播放 480p GIF） |

### 内存（Memory）

| NFR ID | 指标摘要 | 是否量化 | 验收方式 | 备注 |
|---|---|---|---|---|
| NFR-MEM-001 | 2 亿像素图全流程 RSS 增量 ≤ 512MB（4GB RAM 设备） | 是 | Memory Profiler，确认无 OOM | 达标；注意 spec 已标注"具体上限待技术方案确定后可细化" |
| NFR-MEM-002 | 关闭查看器后 30 秒内完全释放��源，无内存泄漏 | 是 | LeakCanary + Profiler | 达标 |
| NFR-MEM-003 | onLowMemory 后 500ms 内释放 ≥ 50% Tile 缓存 | 是 | 模拟 onLowMemory + Profiler 测量 | 达标 |

### 安全与隐私（Security/Privacy）

| NFR ID | 指标摘要 | 是否量化/可验收 | 验收方式 | 备注 |
|---|---|---|---|---|
| NFR-SEC-001 | 不收集用户数据，不申请额外权限 | 可验收（静态审查） | 代码审查 + manifest 权限清单检查 | 达标；建议在 tasks.md 中添加权限审查 Task |
| NFR-SEC-002 | 数据源只读，不复制到 SDK 私有目录 | 可验收（代码审查） | 代码审查 + 文件系统验证（无新增 SDK 私有文件） | 达标 |
| NFR-SEC-003 | Release 构建不输出含敏感信息日志 | 可验收 | ProGuard/R8 构建后 Logcat 抓取验证 | 达标 |

### 可观测性（Observability）

| NFR ID | 指标摘要 | 是否量化/可验收 | 验收方式 | 备注 |
|---|---|---|---|---|
| NFR-OBS-001 | 暴露加载开始/成功/失败/Tile 解码失败回调 | 可验收（接口测试） | 单元测试覆盖各回调路径触发 | 达标 |
| NFR-OBS-002 | Debug 日志 Tag 统一，Release 默认关闭 | 可验收（构建验证） | Release 构建 Logcat 确认无 `MediaViewerSDK` 日志 | 达标 |
| NFR-OBS-003 | 性能数据回调（可选订阅）含解码耗时/缓存命中率/内存占用 | 可验收（接口测试） | 订阅回调后验证字段不为空且值合理 | 达标 |

### 可靠性（Reliability）

| NFR ID | 指标摘要 | 是否量化/可验收 | 验收方式 | 备注 |
|---|---|---|---|---|
| NFR-REL-001 | 快速切图 100 次不崩溃，内存无持续增长 | 是 | 自动化 UI 测试（与 FEAT-002 集成后执行） | 达标；需在 tasks.md 中标注依赖 FEAT-002 |
| NFR-REL-002 | onLowMemory 不 ANR（主线程 < 5 秒阻塞），不崩溃 | 是 | 模拟 onLowMemory + ANR 检测 | 达标 |
| NFR-REL-003 | 解码失败路径有单元测试覆盖，无未捕获异常 | 可验收（测试覆盖） | 单元测试覆盖率报告，失败路径分支覆盖 100% | 达标 |

---

## 三、边界场景覆盖检查

| 边界场景 | spec 中是否有覆盖 | 覆盖位置 | 备注 |
|---|---|---|---|
| 格式不支持（如 TIFF、RAW） | 是 | 边界与异常场景 | |
| 文件损坏（文件头损坏/数据截断） | 是 | 边界与异常场景 | |
| 加载超时（网络图片） | 是 | 边界与异常场景 | 明确超时由加载框架负责，SDK 维持 loading |
| 内存不足（onLowMemory） | 是 | 边界与异常场景 + NFR-MEM-003 + NFR-REL-002 | |
| 快速连续手势（双击/捏合到边界） | 是 | 边界与异常场景 | |
| 超出缩放上限（5×）继续捏合 | 是 | 边界与异常场景 | |
| 低于缩放下限（1×）继续缩小 | 是 | 边界与异常场景 | |
| 图片未放大时尝试平移 | 是 | 边界与异常场景 | |
| 快速连续切换图片 100 次 | 是 | 边界与异常场景 + NFR-REL-001 | 注意需 FEAT-002 集成后验证 |
| HEIF 在 API 26 设备上调用 | 是 | 边界与异常场景 + FR-003 | |
| 动态 WebP 播放 | 是 | FR-004 + AC-004 | |
| GIF 视口外停止播放 | 是 | FR-006 + AC-006 | |
| Tile 解码失败（非文件损坏，如内存不足时 OOM） | 部分 | NFR-REL-003 提到单元测试覆盖 | **建议补充**：Tile 单个解码失败时的行为（如显示占位符还是重试） |
| 查看器在 loading 中被销毁（生命周期边界） | **未覆盖** | — | **建议补充**：Activity/Fragment 在 loading 或 Tile 解码中被销毁时的资源释放行为 |
| 图片宽或高为 0（损坏元数据） | **未覆盖** | — | **建议补充**：极端图片尺寸处理 |

**边界场景覆盖总结**：主要边界场景覆盖良好。建议在下一版本补充：（1）查看器生命周期销毁时的资源释放行为；（2）Tile 单个解码失败的降级展示策略；（3）图片元数据异常（宽/高为 0）的处理。

---

## 四、依赖完整性检查

| 依赖项 | 类型 | spec 中是否明确描述 | 故障模式是否覆盖 | 备注 |
|---|---|---|---|---|
| 图片加载框架（Glide/Coil 等，接入方注入） | 外部/接入方提供 | 是（依赖关系章节） | 是（返回失败时展示 error placeholder） | 明确了框架不绑定、通过接口注入 |
| Android 系统区域解码能力 | 系统 API | 是（依赖关系章节） | 是（不可用时降级为全图采样解码） | 建议在 plan.md 中进一步明确降级判断逻辑 |
| FEAT-002（Pager 翻页容器） | 下游依赖 Feature | 是（下游影响 + NFR-REL-001） | 部分（NFR-REL-001 需 FEAT-002 集成后验证） | **注意**：FR-011 中提及的手势冲突消解扩展���需要在设计阶段与 FEAT-002 联合确认 |
| FEAT-007（幻灯片） | 下游依赖 Feature | 是（下游影响） | — | 幻灯片场景下的性能需求建议在 FEAT-007 spec 中单独定义 |
| FEAT-009（事件回调） | 下游依赖 Feature | 是（下游影响） | — | 回调接口设计需与 FEAT-009 保持一致，建议在 epic-plan 阶段统一接口规范 |
| error placeholder UI（接入方提供） | 接入方提供 | 是（FR-014 + 假设约束） | 是（接入方未实现时 SDK 不崩溃） | |
| loading UI（接入方提供） | 接入方提供 | 是（FR-013 + 假设约束） | 是（接入方未实现时 SDK 不崩溃） | |

**依赖完整性总结**：核心依赖均已描述且有故障模式覆盖。重点关注：FEAT-002 手势冲突消解扩展点需在设计阶段（epicdesign 阶段）明确，避免后期返工。

---

## 五、整体质量评分与结论

| 检查维度 | 状态 | 说明 |
|---|---|---|
| FR 可测试性 | 通过（有待办） | 16 条 FR 均有明确输入/输出描述；FR-013/FR-015/FR-016 缺 AC，需补充 |
| NFR 量化程度 | 通过 | 所有 NFR 均有量化指标和验收方式 |
| AC 与 FR/NFR 的追溯关系 | 通过（有待办） | 主要 AC 均有 FR/NFR 引用；部分 FR 缺独立 AC |
| 边界场景覆盖 | 通过（有待办） | 主要边界已覆盖；建议补充生命周期销毁、Tile 局部失败、元数据异常场景 |
| 依赖完整性 | 通过 | 所有依赖均已描述，故障模式清晰 |
| 禁止实现细节 | 通过 | spec 未出现 API 名称、类名等实现细节 |

**结论**：FEAT-001 spec.md v0.1.0 达到进入 Plan 阶段的基本质量要求。建议在 Plan 阶段开始前，将上述"待补充"项（FR-013/FR-015/FR-016 的 AC，生命周期销毁边界场景，Tile 局部失败策略）纳入下一个 spec 修订迭代，或在 plan.md 中作为技术约束显式说明。

---

*本清单由 AI 辅助生成，最终质量由负责 Feature 的工程师确认。*
