# 需求质量清单：FEAT-007 幻灯片自动播放

**Feature ID**：FEAT-007
**检查时间**：2026-03-22
**检查人**：自动生成（Agent）
**对应 spec 版本**：v0.1.0

---

## 一、完整性检查

| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| C-01 | 所有 FR 均有对应 AC | 通过 | FR-001~010 均有对应 AC-058~073 |
| C-02 | 所有 AC 均可追溯到 FR 或 NFR | 通过 | 每条 AC 括号内标注了来源 FR/NFR ID |
| C-03 | 覆盖功耗 NFR | 通过 | NFR-POWER-001 |
| C-04 | 覆盖内存 NFR | 通过 | NFR-MEM-001、NFR-MEM-002 |
| C-05 | 覆盖性能 NFR | 通过 | NFR-PERF-001、NFR-PERF-002 |
| C-06 | 覆盖安全隐私 NFR | 通过 | NFR-SEC-001 |
| C-07 | 覆盖可观测性 NFR | 通过 | NFR-OBS-001 |
| C-08 | 覆盖可靠性 NFR | 通过 | NFR-REL-001、NFR-REL-002 |
| C-09 | 依赖关系（上游/下游）已明确 | 通过 | FEAT-002、FEAT-006 均有接口契约说明 |
| C-10 | 范围外（Out of Scope）已明确 | 通过 | Indicator 渲染、Pager 动画、后台切换行为均已排除 |
| C-11 | 核心用户旅程 ≥ 2 个 | 通过 | 旅程 1（开启自动播放）、旅程 2（手动暂停恢复）|
| C-12 | 边界与异常场景已列举 | 通过 | 空列表、单 Item、极小 intervalMs、重复调用、生命周期销毁、视频页 |
| C-13 | 核心实体已定义（涉及数据） | 通过 | SlideshowConfig、SlideshowState、PageProgress |
| C-14 | 假设与约束已填写 | 通过 | 包含接口假设、平台约束、可选能力约束 |
| C-15 | 需求追溯表已预留 | 通过 | 所有 FR/NFR 均已列入追溯表待回填 |

---

## 二、可测试性检查

| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| T-01 | 每条 FR 描述可被自动化测试验证（无模糊动词） | 通过 | 所有 FR 使用"必须"、具体动作与可观测结果描述 |
| T-02 | 每条 NFR 含量化指标与验收方式 | 通过 | 各 NFR 均标注了阈值（≤ 16 ms、≤ 50 ms、≤ 1 MB、≤ 0.02 等）及验收工具 |
| T-03 | AC 中时间/数值边界明确 | 通过 | AC-058（±50 ms）、AC-059（≥ 60 次/秒）、AC-067（≤ 0.02）均有量化 |
| T-04 | 边界场景有明确的期望行为（可断言） | 通过 | 每个边界场景均给出"不崩溃"、"静默忽略"、"日志告警"等可观测期望 |
| T-05 | 旅程中的验收场景格式规范（前提/当/则） | 通过 | 两个旅程共 7 个验收场景均使用标准格式 |

---

## 三、一致性检查

| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| CON-01 | AC 编号与 EPIC 上下文提供的关键 AC 一致 | 通过 | AC-058~070 均已覆盖，新增 AC-071~073 补充边界场景 |
| CON-02 | FR 与范围（In Scope）描述一致，无遗漏 | 通过 | 范围内所有能力点均有对应 FR 覆盖 |
| CON-03 | 范围外（Out of Scope）未在 FR 中被实现 | 通过 | Indicator 渲染、Pager 动画未出现在 FR 中 |
| CON-04 | 依赖的 Feature ID 与全局 EPIC 上下文一致 | 通过 | FEAT-002、FEAT-006 与 EPIC 上下文描述一致 |
| CON-05 | `resumeDelayMs` 默认值在 FR/NFR/AC/假设中保持一致（10000 ms） | 通过 | FR-005、AC-061、假设中均为 10000 ms |

---

## 四、风险与待澄清项

| # | 风险/待澄清项 | 优先级 | 建议处理方式 |
|---|--------------|--------|-------------|
| R-01 | `intervalMs` 最小保护阈值具体数值未最终确定（spec 中建议 500 ms，但标注"Plan 阶段确认"） | 中 | 在 Plan 阶段与技术负责人确认最小值，并更新 AC-071 中的具体数值 |
| R-02 | FEAT-002 翻页 API 的具体接口签名（`scrollToPage` 或其他）尚未在 spec 层定义，为假设 | 中 | Plan 阶段读取 FEAT-002 的 plan.md 确认接口契约，若不一致需 CR |
| R-03 | FEAT-006 IndicatorAdapter 进度推送接口（`onProgressUpdate` 或其他）为假设 | 中 | Plan 阶段与 FEAT-006 协同确认接口签名 |
| R-04 | 后台/前台切换（App 进入后台）时幻灯片行为被标记为"Out of Scope"，但接入方可能期望后台暂停 | 低 | 在 EPIC 阶段或后续迭代中评估是否纳入；当前版本行为未定义，建议接入方在 `onPause` 中手动调用 `stopSlideshow` |
| R-05 | Indicator 进度推送频率 ≥ 60 次/秒（NFR-PERF-001）在低端设备（API 24）上的可行性需验证 | 中 | Plan 阶段进行 Android 8.0 设备实测；若无法满足，可降级为 30 次/秒并同步更新 NFR |
| R-06 | `NFR-REL-002` 要求恢复后首帧进度误差 ≤ 0.02，计时精度依赖 `intervalMs` 与系统调度抖动，需在 Plan 阶段评估协程/Handler 的抖动范围 | 低 | Plan 阶段明确计时实现方案，并在单元测试中验证误差范围 |

---

## 五、总体评估

| 维度 | 评级 | 备注 |
|------|------|------|
| 完整性 | 高 | 所有必填��均已填写，FR/NFR/AC 覆盖完整 |
| 可测试性 | 高 | NFR 量化、AC 有数值边界、场景格式规范 |
| 一致性 | 高 | AC 编号与 EPIC 上下文一致，内部无矛盾 |
| 风险 | 中 | 存在 2 个接口假设（R-02、R-03）和 1 个阈值待确认项（R-01），均可在 Plan 阶段解决 |

**结论**：spec.md 质量满足进入 Plan 阶段的要求。建议 Plan 阶段优先解决 R-01（最小保护阈值）、R-02（FEAT-002 接口契约）、R-03（FEAT-006 接口契约）三项待澄清项。
