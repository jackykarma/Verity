# 需求质量清单：FEAT-003 视频 Item 播放

**Feature**：FEAT-003 - 视频 Item 播放
**Epic**：EPIC-001 - Android 媒体浏览 SDK
**评审日期**：2026-03-22
**评审人**：（待填写）
**清单版本**：v0.1.0

> 说明：本清单用于 spec.md 提交前的自检与同行评审。每项标注 [x] 表示已满足，[ ] 表示待处理，[N/A] 表示不适用。

---

## 一、完整性检查

### 1.1 文档结构完整性

- [x] spec.md 包含"背景与价值"章节，含背景、目标、价值、范围（In）、范围外（Out）
- [x] spec.md 包含"依赖关系"章节，含上游依赖、下游影响、外部依赖故障模式
- [x] spec.md 包含"验收与场景"章节，含核心用户旅程（≥2 个）和边界与异常场景
- [x] spec.md 包含"FR / NFR"章节，FR 和 NFR 均已填写
- [x] spec.md 包含"验收标准（Feature Level）"章节
- [x] spec.md 包含"核心实体"章节
- [x] spec.md 包含"假设与约束"章节
- [x] spec.md 包含"需求追溯"章节（预留表格）
- [x] spec.md 包含"��更记录"章节

### 1.2 元数据完整性

- [x] Feature ID、Feature 名称、Epic 引用已填写
- [x] Feature Version 已填写（v0.1.0）
- [x] 创建时间已填写（2026-03-22）
- [x] 状态已填写（草稿）
- [x] EPIC 分支已填写

---

## 二、FR 质量检查

### 2.1 可测试性

| FR ID | FR ���要 | 可测试 | 备注 |
|---|---|---|---|
| FR-001 | 进入视口展示封面/占位背景 | [x] | 可通过 UI 截图/自动化验证 |
| FR-002 | 自动播放模式下进视口自动播放 | [x] | 可 Mock VideoPlayerController 验证 play() 调用 |
| FR-003 | 手动播放模式下不自动播放 | [x] | 可 Mock VideoPlayerController 验证 play() 不被调用 |
| FR-004 | 离开视口通知暂停 | [x] | 可 Mock VideoPlayerController 验证 pause() 调用 |
| FR-005 | 后台暂停，前台不自动恢复 | [x] | 可通过生命周期事件驱动测试验证 |
| FR-006 | 同一时刻最多一个视频播放（单播策略） | [x] | 可通过并发触发多视频播放验证只有一个 play() 生效 |
| FR-007 | 接入方注入自定义播放器 | [x] | 可通过注入 Mock Controller 验证调用链 |
| FR-008 | SDK 不渲染控制栏 | [x] | 可通过 Layout Inspector 或视图层级断言验证 |
| FR-009 | 支持三种数据源类型 | [x] | 可分别用三种类型的 VideoSource 驱动测试 |
| FR-010 | 双指缩放/单指平移/忽略旋转手势 | [x] | 可通过手势事件注入测试 |
| FR-011 | 首帧就绪前持续展示封面（防黑屏） | [x] | 可通过录屏逐帧分析验证 |
| FR-012 | 发布 6 类播放状态事件 | [x] | 可通过事件总线订阅验证各事件触发 |

### 2.2 完备性

- [x] 所有 AC-028 ~ AC-035 及 AC-014、AC-044 均已映射到对应 FR
- [x] 覆盖了封面展示、自动/手动播放触发、暂停（离页/后台）、单播策略、播放器注入、控制栏约束、数据源类型、手势、防黑屏、事件回调共 12 条 FR
- [x] 无遗漏的核心业务场景（参照"范围 In Scope"逐项核对）
- [x] FR 编号连续，无跳号

### 2.3 表述规范

- [x] 每条 FR 使用"系统必须"或"接入方必须能够"等可测试动词
- [x] ��"提升体验"、"更流畅"等不可验证的模糊表述
- [x] FR 描述聚焦"什么行为"，不包含实现细节（如具体类名、算法）

---

## 三、NFR 质量检查

### 3.1 量化与可验证性

| NFR ID | 类别 | 指标是否量化 | 验收方式是否明确 | 备注 |
|---|---|---|---|---|
| NFR-PERF-001 | 性能 | [x] 100ms p95 | [x] Android Profiler / 自动化截帧 | 区分本地缓存与网络封面 |
| NFR-PERF-002 | 性能 | [x] 200ms p95 | [x] Mock Controller 计时 | 自动播放触发延迟 |
| NFR-PERF-003 | 性能 | [x] 不超过 3fps 影响 | [x] 指定测试设备档次 | 明确测试设备 SoC 基准 |
| NFR-POWER-001 | 功耗 | [x] 1mAh/小时增量上限 | [x] Battery Historian | 明确 SDK 自身不含播放器 |
| NFR-POWER-002 | 功耗 | [x] 无后台任务/轮询 | [x] Systrace/Battery Historian | 封面状态下验证无 Wakelock |
| NFR-MEM-001 | 内存 | [x] 2 秒内释放 | [x] Android Profiler | 快速滑过场景 |
| NFR-MEM-002 | 内存 | [x] 基线 + 20MB | [x] 自动化内存测试 | 50 页连续翻页 |
| NFR-SEC-001 | 安全 | [x] 不上传数据 | [x] 网络抓包验证 | 可在代码审查中验证 |
| NFR-SEC-002 | 安全 | [x] 最小权限 | [x] 权限声明审查 | 无持久化存储路径 |
| NFR-OBS-001 | 可观测性 | [x] Debug 级日志，覆盖关键事件 | [x] 日志输出集成测试 | 列举了具体场景 |
| NFR-OBS-002 | 可观测性 | [x] Error 级日志，含错误码 | [x] 错误场景触发后验证日志 | 路径/URL 脱敏 |
| NFR-OBS-003 | 可观测性 | [x] 6 类事件全覆盖 | [x] 事件订阅集成测试 | 引用 FR-012 |
| NFR-REL-001 | 可靠性 | [x] 100 次翻页无崩溃 | [x] 自动化 UI 测试 | 视频 Item 快速翻页专项 |
| NFR-REL-002 | 可靠性 | [x] 20 次前后台切换无崩溃 | [x] 自动化 UI 测试 | 后台切换专项 |
| NFR-REL-003 | 可靠性 | [x] 未注入播放器不崩溃 | [x] 集成测试（空 Controller） | 降级展示封面 |
| NFR-REL-004 | 可靠性 | [x] 数据源不可达不崩溃 | [x] 集成测试（Mock 错误源） | 发布 VideoError 事件 |

### 3.2 NFR 覆盖类别完整性

- [x] 性能（Performance）：已覆盖封面渲染延迟、播放触发延迟、翻页帧率
- [x] 功耗（Power）：已覆盖播放期间 SDK 自身功耗、非播放状态无额外唤醒
- [x] 内存（Memory）：已覆盖快速滑过资源释放、连续翻页内存增量上限
- [x] 安全与隐私（Security/Privacy）：已覆盖数据不上传、最小权限、不持久化
- [x] 可观测性（Observability）：已覆盖调试日志、错误日志、事件回调
- [x] 可靠性（Reliability）：已覆盖快速翻页、前后台切换、缺失播放器、数据源错误 4 个关键场景

---

## 四、验收标准（AC）质量检查

### 4.1 AC 可测试性

- [x] 每条 AC 均为"可验证"表述（含具体可观察的行为或指标）
- [x] 每条 AC 均引用了对应的 FR 或 NFR ID
- [x] 每条 AC 标注了原始 AC 编号来源（如 AC-028 等）
- [x] AC 编号连续（AC-001 ~ AC-015），无跳号

### 4.2 原始 AC 覆盖核对

| 原始 AC | 描述摘要 | 已映射到 spec AC | 备注 |
|---|---|---|---|
| AC-028 | 进入视口显示封面/首帧 | AC-001（FR-001） | 已覆盖 |
| AC-029 | 自动播放时进视口自动播放 | AC-002（FR-002） | 已覆盖 |
| AC-030 | ��动播放时不自动播放 | AC-003（FR-003） | 已覆盖 |
| AC-031 | 离开 Item 后通知暂停 | AC-004（FR-004） | 已覆盖 |
| AC-032 | 后台暂停不继续播放 | AC-005（FR-005） | 已覆盖 |
| AC-033 | 同一时刻只有一个视频播放 | AC-006（FR-006） | 已覆盖 |
| AC-034 | 注入自定义播放器后使用该播放器 | AC-007（FR-007） | 已覆盖 |
| AC-035 | SDK 不渲染默认控制栏 | AC-008（FR-008） | 已覆盖 |
| AC-014 | 视频 Item 双指缩放不模糊 | AC-010（FR-010） | 已覆盖 |
| AC-044 | 双指旋转不触发旋转不崩溃 | AC-011（FR-010） | 已覆盖 |

---

## 五、依赖关系检查

- [x] 上游依赖（FEAT-002、FEAT-009、接入方播放器）均已列明
- [x] 每个上游依赖均描述了"缺失/故障"时的降级行为
- [x] 下游影响（接入方控制 UI、FEAT-002 单播协调）已说明
- [x] 对 FEAT-009 横切能力的依赖已在"依赖关系"章节的说明框中注明，未在本 Feature 中重复定义事件结构

---

## 六、范围边界检查

- [x] 范围外（Out of Scope）与范围内（In Scope）无重叠或矛盾
- [x] 视频控制栏 UI 明确排除在范围外
- [x] 视频格式兼容性明确由接入方播放器负责，SDK 不约束
- [x] 实况图内嵌视频已明确指向 FEAT-004
- [x] DRM、字幕、流媒体协议已明确排除
- [x] per-item 播放模式配置已明确排除
- [x] FR 内容未超出 In Scope 范围

---

## 七、核心实体检查

- [x] VideoSource：含义、核心属性（数据源类型、地址、可选封面 URI）、与 VideoPlayerController 的使用关系已描述
- [x] VideoPlayerController：含义（抽象接口）、主要职责（播放源设置、生命周期控制、视图绑定）、接入方实现关系已描述
- [x] 两个实体均未写入实现细节（无具体类名、方法签名、数据结构定义）

---

## 八、假设与约束检查

- [x] 假设列出了"假设不成立时的降级行为"（FEAT-002 未就绪 → 封面模式；播放器未注入 → 封面模式）
- [x] 平台版本约束已明确（API 24 ~ API 35）
- [x] SDK 不引入具体播放框架依赖的架构约束已明确
- [x] 手势支持边界（缩放+平移，不含旋转）已明确
- [x] 单播和后台暂停为 SDK 强制行为（不可被接入方覆盖）已明确

---

## 九、待澄清问题（spec 草稿阶段）

| # | 问题描述 | ��响范围 | 优先级 | 状态 |
|---|---|---|---|---|
| Q-001 | 自动播放模式下，"完全进入视口"的判断标准是什么？是 Item 可见面积超过某阈值（如 80%），还是严格要求完全可见？ | FR-002，NFR-PERF-002 | 高 | 待澄清 |
| Q-002 | 视频 Item 缩放后（放大状态），离开视口时是否需要重置缩放比例？还是保留用户的缩放状态？ | FR-004，FR-010 | 中 | 待澄清 |
| Q-003 | 快速翻页场景下，若一个视频 Item 进入视口但尚未开始播放（自动播放调度队列中），此时 Item 已滑出，是否需要取消播放调度？取消逻辑的超时阈值是多少？ | FR-002，FR-004，NFR-REL-001 | 高 | 待澄清 |
| Q-004 | NFR-MEM-002 中"基线 + 20MB"的基线定义：是 SDK 初始化后的内存基线，还是不含视频 Item 的普通列表浏览基线？ | NFR-MEM-002 | 中 | 待澄清 |
| Q-005 | VideoPlayerController 是��要求接入方为每个视频 Item 提供独立实例，还是允许复用同一个 Controller 实例？若复用，SDK 的单播策略如何与之协调？ | FR-006，FR-007 | 高 | 待澄清 |

---

## 十、评审结论

| 维度 | 结论 | 备注 |
|---|---|---|
| 完整性 | 通过 | 所有模板章节已填写 |
| FR 可测试性 | 通过 | 12 条 FR 均可测试 |
| NFR 量化 | 通过 | 16 条 NFR 均含量化指标与验收方式 |
| 原始 AC 覆盖 | 通过 | 10 条原始 AC 全部映射 |
| 范围边界 | 通过 | In/Out 边界清晰，无重叠 |
| 待澄清问题 | 待处理 | 5 条待澄清问题需在进入 Plan 阶段前解答 |
| **总体结论** | **草稿就绪，待澄清问题解答后可进入 Plan 阶段** | |
