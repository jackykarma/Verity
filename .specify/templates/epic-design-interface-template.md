# 接口设计：EPIC-[编号] - [EPIC 名称]

> **定位**：本文件对应 `epic-design.md` **§十**，面向方案评审，完整描述本 EPIC 对外提供的接口与依赖的外部接口，与各 Feature 的 `plan.md` §四「接口与契约规范」对齐，确保接口契约在评审阶段得到充分论证。
>
> **互指**：`epic-design.md` §十仅保留定位说明、可选摘要与指向本文件的链接；**正文以本文件为准**。
>
> **与 plan.md 的关系**：各 Feature 的 `plan.md` §四（含 §4.1/§4.2）中声明了**能力承诺与接口约束规则**（对外提供什么能力、依赖什么外部接口、约束规则），本文件在此基础上完成**具体接口设计**（方法签名、参数、返回值、错误码等）。

**Epic**：EPIC-[编号] - [名称]
**关联文件**：`epic-design.md` | 各 `features/*/plan.md` §四
**创建/更新日期**：[YYYY-MM-DD]

---

### 10.1 对外提供的接口

> **适用场景**：本 EPIC 对其他模块、Feature 或外部系统暴露的接口（SDK API、服务接口、组件接口、进程间通信等）。若无对外接口标注 N/A。

#### 10.1.1 接口清单


| 接口ID    | 接口名称   | 接口类型                   | 所属模块/Feature | 消费方        | 规约位置                   |
| ------- | ------ | ---------------------- | ------------ | ---------- | ---------------------- |
| API-001 | [接口名称] | [REST/SDK/IPC/函数调用/事件] | FEAT-xxx     | [消费方模块/外部] | FEAT-xxx plan.md §四（能力承诺与约束） |


#### 10.1.2 关键接口详述

> 仅对评审中需要重点论证的关键接口进行展开描述，常规接口在 plan.md 中维护即可。

##### API-001：[接口名称]

- **接口类型**：REST API / SDK 函数 / IPC / 函数调用 / 事件
- **所属模块**：[模块名称]
- **消费方**：[调用方模块或系统]
- **接口描述**：[一句话说明接口用途]
- **请求/调用**：

```
[方法 / 函数签名 / 请求路径]
参数：
  - [参数名]：[类型] — [说明]
  - [参数名]：[类型] — [说明]
```

- **响应/返回值**：

```
[返回类型]
字段：
  - [字段名]：[类型] — [说明]
```

- **错误码/异常**：


| 错误码   | 含义   | 处理建议   |
| ----- | ---- | ------ |
| [错误码] | [含义] | [处理建议] |


- **并发/幂等/版本约束**：[如：幂等、限流、版本兼容性说明]
- **能力承诺与约束**：→ `FEAT-xxx plan.md §四（§4.1 能力承诺/§4.2 外部依赖约束）`

##### API-002：[接口名称]

（结构同 API-001）

### 10.2 依赖的外部接口

> **适用场景**：本 EPIC 依赖的外部系统、后端服务、第三方 SDK、OS 能力等提供的接口。与 `epic-design.md` §4.4 外部依赖清单对应，本节聚焦接口层面的契约描述。若无外部接口依赖标注 N/A。

#### 10.2.1 外部接口清单


| 接口ID    | 接口名称   | 接口类型             | 提供方        | 所属 Feature | 规约位置                   |
| ------- | ------ | ---------------- | ---------- | ---------- | ---------------------- |
| EXT-001 | [接口名称] | [REST/SDK/系统API] | [提供方团队/系统] | FEAT-xxx   | [文档链接 / plan.md §四（§4.2 外部依赖约束）] |


#### 10.2.2 关键外部接口详述

> 仅对评审中需要重点论证的关键外部依赖接口进行展开，重点说明契约假设与故障应对。

##### EXT-001：[接口名称]

- **接口类型**：REST API / SDK / 系统 API
- **提供方**：[团队名称 / 系统]
- **接口描述**：[一句话说明接口用途]
- **请求/调用**：

```
[方法 / 函数签名 / 请求路径]
关键参数：
  - [参数名]：[类型] — [说明]
```

- **响应/返回值**：

```
关键字段：
  - [字段名]：[类型] — [说明]
```

- **契约假设**：[SLA 约定、响应时延预期、数据一致性假设等]
- **故障模式与应对**：[超时/限流/不可用时的降级或重试策略，与 `epic-design.md` §4.4 对应]
- **依赖约束与降级策略**：→ `FEAT-xxx plan.md §四（§4.2 外部依赖约束）` / [外部文档链接]

##### EXT-002：[接口名称]

（结构同 EXT-001）

### 10.3 接口一致性自检

- 所有对外接口（§10.1）已在对应 Feature 的 `plan.md` §四 中有完整规约定义
- 所有外部接口依赖（§10.2）与 `epic-design.md` §4.4 外部依赖清单条目一一对应
- 关键接口的错误码/异常场景已在 `epic-design.md` §6.2 边界与异常场景中覆盖
- 接口版本兼容性已在 `nfr.md` §9.6 兼容性评估中说明（如适用）
