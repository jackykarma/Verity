# EPIC 技术规格书（Tech Spec）：EPIC-[编号] - [EPIC 名称]

> **定位**：本 EPIC **唯一**的技术规约事实源。只冻结约束与边界，不承载详细设计（架构图、类图、时序、接口字段、表结构、Story 拆解由 `/aisdd.epicdesign` 产出）。
>
> **禁止写入**：0/1 层架构图、组件清单、类图、时序图、流程图、接口方法签名、数据库表字段、Story 拆解、L2 设计、NFR 预算表（→ `nfr.md`）、接口契约细节（→ `interface-design.md`）。
>
> **输入**：`epic.md`、各 `features/*/spec.md`、现有工程代码、`.specify/memory/constitution.md`；各 Feature `ux-design.md`（若存在，由 `/aisdd.featureuidesign` 产出）为可选输入。
>
> **产出命令**：`/aisdd.techspec`。

**Epic**：EPIC-[编号] - [名称]
**Epic Version**：v0.1.0（来自 `epic.md`）
**Tech Spec Version**：v0.1.0
**创建/更新日期**：[YYYY-MM-DD]

---

## Tech Spec 前置检查

| 检查项 | 结论 |
|--------|------|
| 已阅读 `epic.md`，提取 Feature 列表、跨 Feature 技术策略 | 是 / 否 |
| 已阅读**所有** Feature 的 `spec.md` | 是 / 否 |
| 已完成 EPIC 级与各 Feature 的差距分析（可复用 / 需扩展 / 需新增） | 是 / 否 |
| 已对照现有工程代码校准技术栈与分层 | 是 / 否 |

---

## 变更记录（增量变更）

| 版本 | 日期 | 变更范围 | 变更摘要 | 影响 Feature |
|------|------|----------|----------|--------------|
| v0.1.0 | [YYYY-MM-DD] | 初始 | 初版 | — |

---

# 第一部分：EPIC 级公共规约

> 只记录影响多个 Feature 或实现期不可擅改的公共约束。与 constitution 或现有工程一致时，写「沿用现有工程默认规则」。

## 一、EPIC 级公共约束

| 维度 | 约束 |
|------|------|
| **Language/Version** | [例如：Kotlin 2.x] |
| **UI 框架** | [例如：Jetpack Compose] |
| **构建系统** | [例如：Gradle Kotlin DSL] |
| **最低/目标 API** | [例如：API 24 / API 35] |
| **依赖注入** | [例如：Hilt] |
| **测试框架** | [例如：JUnit、Robolectric] |
| **Target Platform** | [例如：Android 8+] |

**其他工程约束**：[单模块/多模块、源码目录约定；无增量则写「沿用现有工程默认规则」]

---

## 二、跨 Feature 边界与依赖规则

- **分层原则**：[表示层 / 领域层 / 数据层；无增量则「沿用现有工程默认规则」]
- **依赖方向**：[谁可依赖谁；禁止反向依赖]
- **Feature 边界**：[能力归属哪个 Feature；共享能力边界]
- **跨层/跨 Feature 禁止项**：[例如：UI 不得直接依赖 DataSource]

---

## 三、统一运行时约束

- **线程/协程**：[EPIC 级调度、取消、主线程约束；无则 N/A]
- **错误处理**：[统一错误类型、传播、用户提示；无则 N/A]
- **日志/可观测性**：[统一字段、采样、脱敏；无则 N/A]
- **权限/安全/合规**：[统一权限与敏感数据原则；无则 N/A]

---

## 四、数据与存储总约束

- **System of Record 原则**：[权威数据源；无则 N/A]
- **缓存策略边界**：[范围、失效、一致性]
- **迁移/回滚原则**：[Room/File 迁移红线；无则 N/A]
- **详细设计位置**：表结构、字段、索引在 `database-design.md`（`/aisdd.epicdesign nfr`）。

---

## 五、跨 Feature 共享能力识别

| 共享能力名称 | 类型 | Owner Feature | 消费方 Feature | 规约要点 | 详细设计位置 |
|-------------|------|---------------|----------------|----------|--------------|
| [能力名] | Infrastructure | FEAT-001 | FEAT-002 | [边界与约束] | `/aisdd.epicdesign` 后补链接 |

**Feature 规约编写顺序**（Owner 章节须先于 Consumer 定稿）：

| 顺序 | Feature | 依赖（须先定稿的 Feature 章节） |
|------|---------|--------------------------------|
| 1 | FEAT-001 | 无 |
| 2 | FEAT-002 | FEAT-001 |

---

# 第二部分：各 Feature 技术规约

> 每个 Feature 一节。只写**相对第一部分**的增量；无增量写「无增量，遵循第一部分 EPIC 级规约」。不得违反第一部分约束。

---

## Feature FEAT-[编号]：[Feature 名称]

| 字段 | 值 |
|------|-----|
| Feature ID | FEAT-[编号] |
| Feature Version | v0.1.0（来自 `spec.md`） |
| 规约档位 | Lite / Standard |
| 类型 | Product / Capability |

### FEAT-[编号] · 一、规约摘要

- **Feature 目标**：[一句话]
- **现有代码边界**：[可复用 / 需扩展 / 需新增]

### FEAT-[编号] · 二、增量约束

- **额外依赖**：[库/SDK/系统能力 / N/A]
- **错误处理增量**：[ / N/A]
- **日志/可观测性增量**：[ / N/A]
- **兼容性约束**：[ / N/A]

### FEAT-[编号] · 三、能力边界与外部依赖

#### 3.1 本 Feature 对外提供的能力

| 能力 | 消费方 | 边界/约束 | 详细设计位置 |
|------|--------|-----------|--------------|
| [能力名 / N/A] | [Feature] | [幂等/取消/权限] | `/aisdd.epicdesign` 后补链接 |

#### 3.2 本 Feature 依赖的外部能力

| 依赖能力 | 提供方 | 调用约束 | 失败/降级要求 |
|----------|--------|----------|---------------|
| [ / N/A] | [Owner] | [超时/重试] | [可见结果] |

### FEAT-[编号] · 四、数据、NFR 与安全硬约束

- **存储形态**：[Room/DataStore/文件/远端 / N/A]
- **System of Record**：[ / N/A]
- **数据生命周期**：[ / N/A]
- **数据规模与增长**：[ / 待确认]
- **迁移/回滚约束**：[ / N/A]
- **性能/功耗/内存约束**：[Feature 特有；无则遵循 EPIC 默认]
- **权限/隐私/合规**：[ / N/A]

<!-- 复制上方「## Feature FEAT-xxx」整块，为 epic.md 中每个 Feature 各一节 -->

---

## 与 epic.md「跨 Feature 技术策略」的对应

| tech-spec 章节 | epic.md 对应项 |
|----------------|----------------|
| 第一部分 一～四 | 技术约束 |
| 第一部分 五 | 共享能力、编写顺序 |
| 第二部分各 Feature | 各 Feature 技术边界 |
