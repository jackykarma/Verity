# 设计说明：EPIC-[编号] - [EPIC 名称]

> **定位**：设计师/产品为 AI 提供的**结构化上下文**，补充设计稿图片中无法直接提取的信息。本文件聚焦于**关系、行为、意图**——即 AI 仅凭截图看不出来的东西。
>
> **与 design-tokens.json 的分工**：精确数值（色值、间距、字号、圆角、高程）统一写在 `design-tokens.json` 中（模板：`.specify/templates/design-tokens-template.json`）。本文件不重复这些数值。若团队无法提供 design-tokens.json，可在§一中用简要文字概述关键数值。
>
> **存放位置**：`design/design-notes.md`（与设计稿图片同级目录）
>
> **是否必填**：**否**。无此文件时仍可运行 `/aisdd.epicuidesign`（仅凭图片等）；有则显著提升 AI 对关系、行为、动效的理解准确度。
>
> **填写者**：设计师或产品经理（非 AI 生成）

---

## 一、设计体系概述

> 简要说明整体设计体系和风格方向，帮助 AI 建立全局认知。精确数值请写在 `design-tokens.json` 中。

- **设计体系**：[Material 3 / 自定义 / 其他]
- **是否支持 Dark Mode**：[是 / 否 / 后续版本]
- **风格关键词**：[如：简洁、圆润、高对比、卡片式布局]
- **与标准 Material 3 的差异**：[如：按钮圆角更大、使用自定义图标集、底部导航无标签文字]

**若未提供 design-tokens.json**，在此补充关键数值：
- 主色：[如 #1976D2]
- 间距基数：[如 8dp]
- 其他关键约定：[自由文本]

---

## 二、图片文件说明

> **本章节是 AI 最需要的输入**。说明 `design/` 目录下每个图片的内容、归属和状态，以及图片之间的关系。

### 命名约定

推荐命名：`[序号]-[界面简称]-[状态].png`（如 `01-home-normal.png`、`02-home-empty.png`）。

### 文件清单

| 文件名 | 界面名称 | 状态 | 所属 Feature | 补充说明 |
|--------|----------|------|--------------|----------|
| | | [正常/空态/加载中/错误/离线/...] | | |

### 图片间的关系

> 哪些图是同一界面的不同状态？哪些图之间存在页面跳转？**这些关系 AI 无法从单张图片中推断。**

**同一界面的不同状态：**
- `01-home-normal.png`、`02-home-empty.png`、`03-home-loading.png` → 首页的三种状态

**页面跳转关系：**
- `01-home-normal.png` → 点击卡片 → `04-detail-normal.png`
- `04-detail-normal.png` → 点击返回 → `01-home-normal.png`

---

## 三、交互说明

> AI 仅从静态截图无法推断的交互行为。**只需写 AI 看不出来的部分**，显而易见的（如"点击按钮跳转"）无需赘述。

### [界面名称]（对应：[文件名]）

- **入口**：[从哪个界面/什么操作进入此界面]
- **隐式交互**（图片中看不出来的）：
  | 元素 | 操作 | 行为 | 备注 |
  |------|------|------|------|
  | [如：列表项] | [左滑] | [显示删除按钮] | [与系统返回手势冲突时的处理] |
  | [如：长列表] | [滚动到底部] | [自动加载下一页] | [每页 20 条] |
  | [如：搜索栏] | [点击] | [跳转独立搜索页，非原地展开] | |

- **状态切换触发条件**（图片只展示了各状态的样子，但没说明何时切换）：
  - 加载中 → 正常态：[数据返回后]
  - 加载中 → 错误态：[请求失败/超时 3s 后]
  - 加载中 → 空态：[返回数据为空]
  - 错误态 → 加载中：[点击重试按钮]

---

## 四、导航结构

> 应用整体的导航模式。AI 可以从截图识别底部 Tab 的存在，但无法确定返回行为、Deep Link 等。

- **主导航**：[底部 Tab / 侧边栏 / 顶部 Tab / 无]
- **Tab 结构**（若有）：
  | Tab 序号 | 名称 | 图标 | 对应界面 | 默认选中 |
  |----------|------|------|----------|----------|
  | | | | | |

- **返回行为**：
  - 系统返回键：[关闭当前页 / 返回上一层 / 弹确认 Dialog / 其他]
  - 顶栏返回：[同系统返回 / 不同则说明]
  - 手势返回：[支持 / 不支持 / 部分页面禁用]

- **Deep Link**：[是否支持 / URL scheme 规则]

---

## 五、动效与过渡

> **AI 无法从静态截图识别任何动效**，此章节是动效信息的唯一来源。若不填写，AI 只能标注动效为 `❓ 低` 置信度。

| 场景 | 动效类型 | 时长(ms) | 缓动曲线 | 说明 |
|------|----------|----------|----------|------|
| 页面进入 | [SharedAxisX / FadeThrough / 无] | | [EaseInOut / 其他] | |
| 页面退出 | | | | |
| 列表项入场 | [逐项延迟 / 整体淡入 / 无] | | | |
| 状态切换 | [Crossfade / AnimatedVisibility / 无] | | | |
| 按钮点击 | [Ripple / Scale / 无] | | | |
| [自定义] | | | | |

---

## 六、适配策略

> AI 从单张截图只能看到一种屏幕尺寸，无法推断适配策略。

- **屏幕方向**：[竖屏锁定 / 支持横屏 / 部分页面横屏]
- **折叠屏**：[支持 / 不支持 / 后续版本]
- **平板/大屏**：[响应式布局 / 等比放大 / 不考虑]
- **字体缩放**：[支持系统缩放至 xx% / 关键区域固定]
- **刘海屏/挖孔屏**：[内容避让策略]

---

## 七、补充说明

> 任何上述章节未覆盖但对理解设计意图有帮助的信息。

[自由文本]
