# Plan（Feature 轻量技术规约）：[Feature 名称]

**Epic**：EPIC-[编号] - [名称]  
**Feature ID**：FEAT-[编号，例如 001]  
**Feature Version**：v0.1.0（来自 `spec.md`）  
**Plan Version**：v0.1.0  
**当前工作分支**：`epic/EPIC-[编号]-[short-name]`（EPIC 统一分支，不为 Feature 单独建分支）  
**Feature 目录**：`specs/epics/EPIC-[编号]-[short-name]/features/FEAT-[编号]-[short-name]/`  
**日期**：[YYYY-MM-DD]  
**输入**：`spec.md`、`epic-plan.md`（若存在）、`ux-design.md`（若存在）

> **定位**：本文档只记录本 Feature 的**轻量技术规约**，用于在进入设计前冻结必要约束、能力边界与不可擅改的技术决策。它不是设计说明书。
>
> **必须写入 plan 的内容**：相对 EPIC 级约束的增量、外部依赖与调用约束、对外能力边界、数据落点与生命周期、Feature 级 NFR/安全/合规硬约束、需要人工确认的问题。
>
> **不得写入 plan 的内容**：0/1 层架构图、组件协作细节、类图、时序图、流程图、Story 拆解、L2 设计、数据库表字段/索引、接口方法签名、DTO 字段、埋点字段。上述内容统一进入 `/aisdd.epicdesign` 及其子文件。
>
> **裁剪规则**：没有增量约束的章节直接写“无增量，遵循 `epic-plan.md`”；小改动可使用本文档的 Lite 填法，只保留“规约摘要”和“待确认项”。

## 变更记录（增量变更）

| 版本 | 日期 | 变更范围（Feature/Story/Task） | 变更摘要 | 影响模块 | 是否需要回滚设计 |
|---|---|---|---|---|---|
| v0.1.0 | [YYYY-MM-DD] | Feature | 初始版本 |  | 否 |

## Plan 前置检查

| 检查项 | 结论 | 说明 |
|---|---|---|
| 已阅读 `spec.md` | 是 / 否 | 提取 FR/NFR/AC、范围边界、依赖 |
| 已阅读 `epic-plan.md`（若存在） | 是 / 否 / N/A | 若存在则必须读取；单 Feature 或小改动可由本 plan 合并必要 EPIC 级约束 |
| 已检查跨 Feature 共享能力 | 是 / 否 / N/A | 若依赖 Owner Feature，只记录能力边界，不重复设计 |
| 已识别现有代码可复用边界 | 是 / 否 | 只记录复用/扩展/新增边界，不展开实现方案 |
| 是否适用 Lite 填法 | 是 / 否 | 小改动、无增量约束、无跨 Feature 依赖时可选 |

---

## 一、规约摘要

> 3～8 条即可。只写能影响设计与实现的约束结论，不复述需求全文。

- **Feature 目标**：[一句话概括本 Feature 要交付的能力]
- **适用档位**：Lite / Standard（默认） / 需补充 Deep 设计
- **EPIC 级约束来源**：`epic-plan.md` / 本 plan 合并 EPIC 级约束 / N/A
- **现有代码边界**：[可复用模块 / 需扩展模块 / 新增最小单元]
- **关键技术决策**：[若无，写“无额外技术决策”]

---

## 二、Feature 增量约束

> 只写相对 `epic-plan.md` 或项目默认规则的增量。没有增量时统一写“无增量，遵循 `epic-plan.md`”。

- **额外依赖**：[新增库/SDK/系统能力；无则写 N/A]
- **线程/并发约束**：[仅写本 Feature 特有规则；无则写 N/A]
- **错误处理增量**：[特有错误类型、降级要求；无则写 N/A]
- **日志/可观测性增量**：[特有日志字段或指标口径；无则写 N/A]
- **兼容性约束**：[系统版本、设备形态、升级/回滚要求；无则写 N/A]

## 三、能力边界与外部依赖

> 写“能力级”边界，不写方法签名、参数、返回值、DTO 字段。详细接口设计进入 `interface-design.md` 或 L2。

### 3.1 本 Feature 对外提供的能力

| 能力 | 消费方 | 边界/约束 | 详细设计位置 |
|---|---|---|---|
| [能力名称或 N/A] | [Feature/模块] | [幂等/取消/权限/版本等能力级约束] | `/aisdd.epicdesign` 后补链接 |

### 3.2 本 Feature 依赖的外部能力

| 依赖能力 | 提供方 | 调用约束 | 失败/降级要求 |
|---|---|---|---|
| [外部模块/API/SDK 或 N/A] | [Owner] | [超时/重试/限流/缓存等] | [失败时对用户或调用方的可见结果] |

## 四、数据、NFR 与安全硬约束

> 只写约束，不写设计。表结构、字段、索引、查询、埋点字段进入 design 子文件。

- **存储形态**：Room / DataStore / SharedPreferences / 文件 / 远端服务 / N/A
- **System of Record**：[DB / 文件 / 服务端 / 内存态 / N/A]
- **数据生命周期**：[常驻 / 会话内 / 退出清理 / 后台保留 / N/A]
- **数据规模与增长**：[数量级、读写频次；未知则写待确认]
- **迁移/回滚约束**：[是否涉及 schema 版本、兼容、回滚；无则 N/A]
- **性能/功耗/内存约束**：[Feature 特有目标；无则遵循 EPIC 级预算]
- **权限/隐私/合规约束**：[权限申请、敏感数据、脱敏、用户同意；无则 N/A]

## 五、Design 输入清单

> 本节只列后续 `/aisdd.epicdesign` 必须展开的设计点，避免在 plan 阶段提前写详细方案。

| 设计点 | 为什么需要设计 | 建议产出位置 |
|---|---|---|
| [例如：本地缓存一致性 / N/A] | [风险、复杂度或跨模块原因] | `key-func-design/KD_*` / `database-design.md` / `interface-design.md` / `l2_design/ST-*` |

## 六、待确认问题

| ID | 问题 | 影响 | Owner | 决策截止点 |
|---|---|---|---|---|
| Q-001 | [待确认问题；无则写 N/A] | [影响的约束/设计/实现] | [PM/UX/Backend/Android] | epicplan / epicdesign / implement |

---

## 复杂度跟踪（仅当突破默认约束时填写）

| 违规项 | 必要性说明 | 舍弃更简单方案的原因 |
|---|---|---|
| [例如：第4个子项目] | [当前需求] | [为何更简单方案不满足] |
