# Plan（Feature 轻量技术规约）：[Feature 名称]

> **定位**：本文档只记录本 Feature 的**轻量技术规约**（增量约束 / 能力边界 / 数据·NFR·安全硬约束）；**不是**设计说明书。
> 详细设计（架构图、类图、时序图、流程图、Story 拆解、L2、表结构 / 字段 / 索引、接口方法签名、DTO 字段、埋点字段）由 `/aisdd.epicdesign` 阶段及其子文件产出。
> **裁剪规则**：没有增量约束的章节直接写「无增量，遵循 `epic-plan.md`」；小改动可仅保留「§一 规约摘要」。
> **按需小节**：若引入复杂度突破最小改动原则（章程要求），按 `.specify/memory/constitution.md` 要求临时追加「复杂度跟踪」小节，说明必要性与取舍；本模板不预置占位。
> **未决事项**：spec/plan 缺口或待确认问题走 `/aisdd.clarify` 或 `/aisdd.cr`，不在 plan 中单开章节。

| 字段 | 值 |
|---|---|
| Epic | EPIC-[编号] - [名称] |
| Feature ID | FEAT-[编号] |
| Feature Version | v0.1.0（来自 `spec.md`） |
| Plan Version | v0.1.0 |
| 当前工作分支 | `epic/EPIC-[编号]-[short-name]`（EPIC 统一分支） |
| 日期 | [YYYY-MM-DD] |
| 输入 | `spec.md`、`epic-plan.md`（若存在）、`ux-design.md`（若存在） |

## 变更记录（增量变更）

| 版本 | 日期 | 变更范围（Feature/Story/Task） | 变更摘要 | 影响模块 | 是否需要回滚设计 |
|---|---|---|---|---|---|
| v0.1.0 | [YYYY-MM-DD] | Feature | 初始版本 |  | 否 |

## Plan 前置检查

| 检查项 | 结论 |
|---|---|
| 已阅读 `spec.md`，提取 FR/NFR/AC、范围边界、依赖 | 是 / 否 |
| 已阅读 `epic-plan.md`（多 Feature）或在本 plan 合并 EPIC 级约束（单 Feature） | 是 / 否 / N/A |
| 已完成现有代码差距分析（可复用 / 需扩展 / 需新增） | 是 / 否 |

---

## 一、规约摘要

> 3～8 条结论。只写能影响设计与实现的约束，不复述需求全文。

- **Feature 目标**：[一句话]
- **EPIC 级约束来源**：`epic-plan.md` / 本 plan 合并 EPIC 级约束 / N/A
- **现有代码边界**：[可复用模块 / 需扩展模块 / 新增最小单元]

---

## 二、Feature 增量约束

> 只写相对 `epic-plan.md` 或项目默认规则的增量；无增量统一写「无增量，遵循 `epic-plan.md`」。

- **额外依赖**：[新增库 / SDK / 系统能力 / N/A]
- **错误处理增量**：[特有错误类型、降级要求 / N/A]
- **日志 / 可观测性增量**：[特有日志字段或指标口径 / N/A]
- **兼容性约束**：[系统版本、设备形态、升级 / 回滚要求 / N/A]

## 三、能力边界与外部依赖

> 写「能力级」边界，**不写**方法签名 / 参数 / 返回值 / DTO 字段（归 `interface-design.md` 或 L2）。

### 3.1 本 Feature 对外提供的能力

| 能力 | 消费方 | 边界 / 约束 | 详细设计位置 |
|---|---|---|---|
| [能力名 / N/A] | [Feature/模块] | [幂等 / 取消 / 权限 / 版本] | `/aisdd.epicdesign` 后补链接 |

### 3.2 本 Feature 依赖的外部能力

| 依赖能力 | 提供方 | 调用约束 | 失败 / 降级要求 |
|---|---|---|---|
| [外部模块/API/SDK / N/A] | [Owner] | [超时 / 重试 / 限流 / 缓存] | [失败时对调用方的可见结果] |

## 四、数据、NFR 与安全硬约束

> 只写**约束**，不写设计。表结构、字段、索引、查询、埋点字段进入 design 子文件。

- **存储形态**：Room / DataStore / SharedPreferences / 文件 / 远端服务 / N/A
- **System of Record**：[DB / 文件 / 服务端 / 内存态 / N/A]
- **数据生命周期**：[常驻 / 会话内 / 退出清理 / 后台保留 / N/A]
- **数据规模与增长**：[数量级、读写频次；未知则写待确认]
- **迁移 / 回滚约束**：[是否涉及 schema 版本、兼容、回滚 / N/A]
- **性能 / 功耗 / 内存约束**：[Feature 特有目标；无则遵循 EPIC 级预算]
- **权限 / 隐私 / 合规约束**：[权限申请、敏感数据、脱敏、用户同意 / N/A]
