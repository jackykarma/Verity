---
description: "当用户输入 /aisdd.epicuidesign 时触发：解析设计稿（交互稿/视觉稿），提取并结构化交互逻辑与视觉规范，产出可验证的 ux-design.md"
alwaysApply: false
---

# /aisdd.epicuidesign — EPIC 设计稿解析

## 触发

用户输入 `/aisdd.epicuidesign`，可附带 EPIC 标识、解析侧重（`交互` / `视觉`）或 Figma 链接。

## 目的

从设计稿中**提取并结构化**交互逻辑与视觉规范，产出 EPIC 根目录下的 `ux-design.md`，供团队验证 AI 对设计稿理解是否完整、正确。**不是** AI 凭空创作 UX 设计。

## 前置条件

1. `epic.md` 已存在
2. **所有 Feature 的 `spec.md` 已完成**（Spec Ready）
3. 可与 `/aisdd.techspec` 并行执行
4. 非技术 Feature（纯后台/数据/SDK）可跳过

## 输入

### 必须阅读

1. **`epic.md`**：背景、范围、Feature 列表
2. **所有 Feature 的 `spec.md`**：用户旅程、FR、NFR、边界场景（作为交叉比对的需求参照）
3. **`.specify/templates/ux-design-template.md`**：模板结构
4. **`design/design-notes.md`**（若存在）：设计师提供的结构化设计上下文——**优先于图片识别结果**
5. **`design/design-tokens.json`**（若存在）：精确设计变量——**优先于图片识别的数值**

### 设计素材（五种输入源，可组合；按置信度从高到低排列）

> **核心原则**：AI 对设计稿的理解精度取决于输入的结构化程度。纯图片识别存在精度瓶颈（间距 ±4dp、色值偏差、动效不可见），因此引入结构化补充输入以提升理解准确度。

1. **设计说明文档**（`design/design-notes.md`）：设计师/产品手写的结构化上下文——**聚焦关系、行为、意图**
   - 模板：`.specify/templates/design-notes-template.md`
   - 覆盖：图片间关系与归属、隐式交互行为、导航结构、动效参数、适配策略
   - **不重复**精确数值（色值/间距/字号等），数值由 design-tokens 承载
   - AI 应**优先采信** design-notes 中的信息，当与图片识别结果冲突时以 design-notes 为准
2. **Design Token 文件**（`design/design-tokens.json`）：从 Figma/设计工具导出的精确设计变量——**聚焦数值**
   - 模板：`.specify/templates/design-tokens-template.json`
   - 覆盖：色值、间距、字号、行高、圆角、高程等**精确数值**
   - 与 design-notes 互补：notes 提供上下文与行为，tokens 提供精确数值
   - AI 应直接引用 Token 值，**不再从截图中猜测**这些数值
3. **Pencil 文件**（`design/` 下的 `.pen`）：通过 Pencil MCP 工具读取
   - `open_document(filePath)` → 打开文件
   - `batch_get(filePath)` → 获取节点树结构
   - `snapshot_layout(filePath)` → 获取布局信息
   - `get_screenshot(filePath, nodeId)` → 获取关键 Frame 截图
4. **图片文件**（`design/` 下的 `.png` / `.jpg` / `.webp`）：通过 Read 工具读取图片进行视觉分析
   - **推荐命名约定**：`[序号]-[界面简称]-[状态].png`（如 `01-home-normal.png`、`02-home-empty.png`）
   - 有命名约定的图片 AI 可自动推断界面归属和状态分组；无规范命名时依赖 design-notes 说明
5. **Figma 链接**：用户在 `$ARGUMENTS` 中提供，或在 `design/figma-links.md` 中索引

### 输入质量分级与置信度

AI 解析完成后，须在元信息中评估输入质量等级，并对每条提取结果标注置信度：

| 输入质量等级 | 条件 | AI 提取的置信度 |
|---|---|---|
| **A — 高置信** | design-notes + design-tokens + 图片齐备 | 大部分条目标 `✅ 高` |
| **B — 中置信** | 有 design-notes 或 design-tokens 之一 + 图片 | 有文字说明的条目 `✅ 高`，纯视觉识别的 `⚠️ 中` |
| **C — 低置信** | 仅有图片（无结构化补充） | 精确数值类（色值/间距）`❓ 低`，布局结构类 `⚠️ 中` |
| **D — 兜底** | 无任何设计素材 | 全部 `[AI 建议]` |

置信度标记规则：
- `✅ 高`：来源于 design-notes/design-tokens 的精确信息，或 Pencil 节点树数据
- `⚠️ 中`：AI 从图片中识别且有较高把握（如明显的布局结构、清晰的文案），或有 spec.md 交叉印证
- `❓ 低`：AI 从图片中推测的精确数值（间距、色值、字号等），或存在多种合理解读

### 兜底模式

当 `design/` 为空且无 Figma 链接时，AI 基于 spec.md 提出 UX 建议草案，所有内容标记为 `[AI 建议 - 待设计师确认]`。

## 输出

EPIC 根目录下的 `ux-design.md`，覆盖模板全部章节：

- **元信息**：Epic、版本、解析模式、设计稿来源清单
- **信息架构**：从设计稿识别的跨 Feature 导航、页面层级、界面清单
- **交互说明（交互稿解析结果）**：页面流转图（Mermaid）、逐屏交互规则、状态定义、反馈方式、异常与边界交互
- **导航与手势**：从设计稿识别的导航模式、返回行为、手势交互
- **视觉规范（视觉稿解析结果）**：主题与色板、布局结构（Markdown 表格展示尺寸与间距）、组件清单、动效/过渡、响应与适配
- **无障碍**：触控区域、对比度、内容描述等
- **设计稿索引**：所有素材索引（含解析状态列）
- **遗漏与待确认**：设计稿未覆盖的场景、AI 待确认项、建议补充内容
- **变更记录**：初始版本 v0.1.0

## 行为约束

- **EPIC 级整体视角**：从整个 EPIC 需求整体解析，保证跨 Feature 一致性
- **输入优先级（高 → 低）**：design-notes.md 文字说明 > design-tokens.json 精确数值 > Pencil 节点树数据 > 图片视觉识别。当不同输入源存在冲突时，采信优先级更高的来源，并在「待确认项」中记录冲突
- **来源与置信度标注**：所有提取的交互规则和视觉规范必须标注来源（文件名/页面/区域）及置信度（`✅ 高` / `⚠️ 中` / `❓ 低`）
- **交叉比对**：必须将提取结果与 spec.md 中的 FR/AC/用户旅程逐条比对，在「遗漏与待确认」章节标出设计稿未覆盖的场景
- **兜底标记**：无设计稿时的 AI 建议内容须明确标记 `[AI 建议 - 待设计师确认]`，区别于从设计稿解析出的确定性内容
- **低置信提示**：当输入质量为 C 级（仅有图片）时，须在完成报告中醒目提示用户补充 `design-notes.md` 或 `design-tokens.json` 以提升准确度
- **模板结构保护**：不得增删章节
- 作为tech-spec.md（第二部分各 Feature 节） 和 `epic-design.md` 的**可选输入**（UI/交互约束）

### spec / ux-design 边界守护（必须）

> 参见 `.cursor/rules/aisdd-document-boundaries.mdc` **Part B**（spec↔ux）；技术细节见 **Part A**。

写入 `ux-design.md` 时，**必须检查**每条内容是否属于 ux-design 的事实源范围。若检测到以下**越界内容**，**禁止直接写入**，必须先向用户列出疑似越界条目并获得明确确认后再写入：

| 越界类型 | 识别特征 | 正确归属 |
|----------|----------|----------|
| 行为契约 | 用「系统必须……」「用户必须能够……」句式定义功能结果 | `spec.md` FR |
| 量化指标 | 含超时阈值、百分比、并发数等可测量数值 | `spec.md` NFR |
| 范围定义 | 描述 In/Out Scope、功能边界 | `spec.md` 背景与价值 |
| 状态进入/退出条件 | 描述「何时进入某状态」的业务规则（而非状态的视觉表现） | `spec.md` 边界与异常场景 |

**提示格式**：

```
⚠️ 边界检查：以下内容疑似属于 spec.md 而非 ux-design.md：

1. [具体条目] → 建议归入 spec.md「[目标章节]」
2. [具体条目] → 建议归入 spec.md「[目标章节]」

请确认：
(a) 移入 spec.md 补充清单（推荐）
(b) 确认保留在 ux-design.md（需说明理由）
(c) 拆分：呈现部分留 ux-design，行为部分移 spec
```

### AI 视觉理解验证图（必须）

> 目的：AI 用绘图展现对设计稿 UI 结构和视觉规范的理解，让开发者可以直观 review AI 是否正确理解了设计意图，减少下游返工。

**执行时机**：完成视觉规范章节的文字提取后、写入 `ux-design.md` 之前或之后。

**必须为以下内容生成理解验证图**：

1. **关键界面布局结构图**：对每个关键界面（至少覆盖界面清单中标记为核心的界面），使用 GenerateImage 工具生成**线框级布局草图**，展现：
   - 界面区域划分（顶栏/内容区/底栏/侧边栏）
   - 关键组件的位置与相对大小关系
   - 间距与对齐标注（概要级）
   - 组件类型标注（如 LazyColumn、TopAppBar、FAB 等）

2. **状态切换示意图**（若涉及多状态界面）：为包含 Loading / 空态 / 错误态 / 正常态等多状态的界面生成**状态对比图**，在一张图中展示各状态的视觉差异。

3. **页面流转缩略图**（可选，推荐）：对核心用户旅程，生成**多屏缩略流程图**，展示从入口到结束的关键界面跳转。

**图片命名**：`ui-understanding-[界面ID或简称].png`，存放于 EPIC 的 `design/ai-understanding/` 目录。

**图片引用**：在 `ux-design.md` 的「AI 视觉理解验证图」章节中引用生成的图片，并标注对应的界面 ID 与设计稿来源。

**review 引导**：每张图下方须附简要文字说明 AI 的理解要点，便于 reviewer 快速比对。格式：

```markdown
**AI 理解要点**：
- 顶部为 [组件]，高度约 [xx]dp
- 内容区为 [组件]，包含 [N] 个 [子项类型]
- 底部 [有/无] 导航栏
- 与设计稿 [design/xxx.png] 对应
```

## 完成后

提示用户：
1. **重点 review AI 视觉理解验证图**（`design/ai-understanding/`），确认 AI 对 UI 结构与视觉规范的理解是否正确
2. **review「遗漏与待确认」章节**，确认 AI 理解是否完整
3. **review 置信度标记**：重点关注 `❓ 低` 和 `⚠️ 中` 的条目，确认或纠正
4. 若 AI 理解有误，指出具体偏差后可增量更新
5. 若有设计稿未覆盖的场景，补充设计稿后可说明更新范围做增量更新
6. 后续 `/aisdd.techspec` 和 `/aisdd.epicdesign` 可引用此文件作为 UI 约束输入

**若输入质量为 C/D 级**，额外提示：
- 建议补充 `design/design-notes.md`（参考模板 `.specify/templates/design-notes-template.md`）提供图片间关系、交互细节、动效参数等 AI 无法从截图推断的信息
- 建议补充 `design/design-tokens.json`（参考模板 `.specify/templates/design-tokens-template.json`）提供精确色值、间距、字号等数值
- 补充后说明更新范围，AI 可做增量更新以提升准确度
