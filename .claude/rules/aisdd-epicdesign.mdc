---
description: "当用户输入 /aisdd.epicdesign 时触发：产出 EPIC 软件设计说明书（通过参数控制本次输出的章节范围，大章节输出到子文件并引用）"
alwaysApply: false
---

# /aisdd.epicdesign — EPIC 软件设计说明书

## 触发

用户输入 `/aisdd.epicdesign`，可附带章节范围参数控制本次输出内容。完整参数与步骤见 `.claude/commands/aisdd.epicdesign.md`。

## 目的

在 EPIC 根目录产出 EPIC 软件设计说明书及配套子文件，作为面向人类评审与后续 Task/Implement 阶段 AI 编码引用的设计方案文档。与各 Feature 的 `plan.md`（技术规约）共同约束 tasks.md 与代码实现。

## 产出文件组（与命令一致）

| 文件 / 目录 | 对应章节 | 内容 |
|-------------|----------|------|
| `epic-design.md` | §1~§6、§7 清单与引用、§8 摘要/引用、§9 摘要+`nfr.md` 链接、§10~§14 | 设计总览；**§7.1** 为 KD 权威清单（含层级、前置 KD、路径）；**§9** 不粘贴 9.1～9.7 全文 |
| `key-func-design/KD_*_*.md` | §七（详细） | 每关键设计一篇；须含**关键类图**（关键类/接口 + 关键字段与方法）与核心时序等；命名 `KD_${三位序号}_${精炼slug}.md` |
| `key-func-design/feature-flows.md` | §七（可选） | Feature 流程图集 |
| `nfr.md` | §九（详细） | 技术评估 9.1～9.7 全文 |
| `key-diagram-epic.md` | §八（EPIC 级） | 全景骨架类图、可选跨 Feature 完整时序；**不再**使用 EPIC 根目录单一 `key-diagram.md` |
| `features/FEAT-xxx/key-diagram.md` | §八（按 Feature） | 该 Feature 子类图（全量签名与变更标识）+ 本 Feature 内完整时序 |
| `features/FEAT-xxx/l2_design/ST-xxx_*.md` | §十四 | L2 详细设计（**每 Story 一文件**；§14 含索引与依赖总览） |

**禁止**：新 EPIC **不得**创建 EPIC 根目录 **`key-func-design.md`**（已废弃）。

## 子文件引用规则示例

**§七**（清单在 epic，正文在 KD 文件）：

```markdown
## 七、关键功能与疑难点/亮点设计

**摘要**：[2～3 句概述 KD 数量与分层]

### 7.1 关键设计清单
（表格含：编号、名称、层级/类型、前置 KD、关联 §八、路径 `./key-func-design/KD_001_xxx.md`）

### 7.2 详细设计引用
- [KD-001 名称](./key-func-design/KD_001_xxx.md)
```

**§九**（正文在 `nfr.md`）：

```markdown
## 九、技术评估（设计产出验证，必须量化）

**摘要**：[2～3 句]

→ 详见 [nfr.md](./nfr.md)
```

**§八、§十四**：§八 为 `key-diagram-epic.md` + 各 Feature `features/FEAT-xxx/key-diagram.md` 的摘要 + 链接；§十四 为各 Feature `l2_design/ST-xxx_*.md` 的索引、前置依赖列与 EPIC 级依赖总览（见 `epic-design-doc-template.md`）。

## 行为约束

- **基于真实代码**：类图/时序图须使用工程真实类名与方法名（`.claude/rules/specify-diagram-requirements.mdc`）。
- **KD 核心方案（必须）**：每个 `key-func-design/KD_*_*.md` 按 `.specify/templates/key-func-design-kd-template.md` 撰写；**全覆盖**技术点、**全链路**、**每环可落地**、与图互证。
- **KD 关键类图（必须）**：每个 KD 须含**关键类图**（真实类/接口名 + 与方案相关的关键字段与方法）；与**核心调用链时序图** participant 一致；**全量**精确化见 §八 **`key-diagram-epic.md`** 与各 **`features/FEAT-xxx/key-diagram.md`**。
- **KD 内流程图（必须）**：每个 KD 的**方案流程图**（及按需架构图）**直接绘制在该 KD 的 `KD_*_*.md` 内**；`feature-flows.md` 仅作跨 KD/跨 Feature 补充，**不得**作为省略 KD 内流程图的替代。
- **KD 依赖（必须）**：`epic-design.md` §7.1 与各 KD 文首「依赖的其他 KD」**一致**；依赖关系须为 **DAG**，**禁止循环依赖**。
- **时序图图后说明（必须）**：凡含 `sequenceDiagram` 的 `key-func-design/KD_*_*.md`、`key-diagram-epic.md`、`features/*/key-diagram.md`、`features/*/l2_design/*.md` 等，每张时序图**紧下方**须有详细协作者与过程说明（`.claude/rules/specify-diagram-requirements.mdc` §四）。
- **Mermaid 样式**：遵循 `.claude/rules/mermaid-style-guide.mdc`。
- **演进式设计**：不得提出整体重写（`.specify/memory/constitution.md`）。
- **子文件自包含**：各 **`KD_*_*.md`**、**`nfr.md`**、`key-diagram-epic.md`、各 **`features/*/key-diagram.md`**、各 **`features/*/l2_design/ST-*.md`** 须自包含可读；允许文内相对链接到其他 KD 或同目录下其他 ST 的 L2 文件。
- **章节骨架先行**：首次调用输出完整 §1～§14 骨架；后续仅更新指定章节范围。

## 输入模板（须读取）

- `epic-design-doc-template.md`、`key-func-design-kd-template.md`、`key-func-design-feature-flows-template.md`（按需）、`nfr-template.md`、`key-diagram-epic-template.md`、`key-diagram-feature-template.md`、`story_detail_design_template.md`
- `specify-diagram-requirements.mdc`、`mermaid-style-guide.mdc`、`constitution.md`

## 完成后

按 `.claude/commands/aisdd.epicdesign.md`「完成报告」提示下一步（key → diagram → nfr → story → l2 → gate / featuretasks）。
