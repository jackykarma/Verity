---
description: Specify/Plan 图表内容规范——基于实际工程架构与真实代码，用于 plan.md、features/*/l2_design/*.md 等设计文档
globs: ["**/*.md"]
alwaysApply: true
---

# Specify 图表内容要求（类图 / 时序图 / 流程图）

本规则与 `.claude/rules/mermaid-style-guide.mdc` 互补：**mermaid-style-guide** 负责视觉样式（配色、主题），本规则负责**内容与结构**要求。

---

## 一、首要原则

- **必须**基于**本工程当前架构模式**和**真实代码**绘制架构图、类图、时序图，不可伪造。
- **禁止**按模板示例（如 Clean Architecture 的 UseCase、Repository）生搬硬套；须按项目实际存在的类与分层绘制。
- 项目架构不限于 MVVM/MVP/MVC/MVI/Clean Architecture，可能是自定义、混合或无名模式——以工程实际为准。

---

## 二、架构适配

**必须按本工程既有架构与真实代码绘制**，不限定于 MVVM/MVP/MVC/MVI/Clean Architecture 等常见模式。若工程采用自定义架构、混合模式或无名模式，同样按其实际分层与类名绘制。

下表仅为**常见架构示例**（非穷举、非强制）：

| 项目架构（示例） | 类/participant 示例 |
|------------------|---------------------|
| MVVM 无 UseCase | ViewModel、Repository、DataSource、Entity、DTO |
| MVP | View、Presenter、Model、DataSource |
| MVC | View、Controller、Model |
| MVI | View、Intent、Model、Reducer 等 |
| Clean Architecture | ViewModel、UseCase、Repository 等 |
| 自定义/混合/其他 | 按项目实际分层与真实类名 |

---

## 三、类图要求

### 全景类图（Feature 级）

- **覆盖范围**：必须覆盖所有关键类/接口，按项目架构确定。
- **方法签名**：类与接口必须写出方法签名（方法名 + 参数 + 返回值）。
- **依赖方向**：上层依赖下层，禁止下层反依赖上层。

### 类图粒度控制

**✅ 应该包含**：公共接口/抽象类、核心实现类、数据模型（Entity、DTO）、错误类型体系、公共方法签名。

**❌ 不应该包含**：私有辅助方法、自动生成的 getter/setter、第三方库的类（除非自定义扩展）、Android SDK 基类。

---

## 四、时序图要求

- **participant 命名**：必须使用**真实类名**，不可伪造类名，不可使用示例中的类名。
- **完整性**：必须覆盖从用户触发到最终响应的完整调用链，不得遗漏关键方法调用。
- **异常**：同图包含正常 + 关键异常（用 `alt/else`）。

### 异常分支覆盖最低标准

生成时序图时，必须逐项判断以下 7 类异常的适用性。**适用且未在图中体现 `alt/else` 分支，视为内容缺失**，不满足完整性要求：

| # | 异常类型 | 适用判断 |
|---|---------|---------|
| 1 | 网络请求失败（超时 / 连接错误 / HTTP 错误码） | 涉及网络调用时适用 |
| 2 | 数据解析 / 类型错误（JSON 异常、类型不匹配） | 涉及数据反序列化时适用 |
| 3 | 本地持久化读写失败（Room SQLiteException / File IO 异常） | 涉及 Room / 文件操作时适用 |
| 4 | 协程取消（用户离开页面 / 系统回收资源导致 CancellationException） | 几乎所有异步 Story 均适用 |
| 5 | 运行时权限被拒绝（Camera / Location / Storage 等） | 涉及危险权限申请时适用 |
| 6 | 数据为空 / 空状态（Empty State，首次加载或搜索无结果） | 涉及列表/详情加载时适用 |
| 7 | 并发竞争（多处同时写同一资源、重复提交同一操作） | 涉及并发写或防重复提交时适用 |

在「协作者与过程说明」的「分支与异常」段中，必须对每类异常逐一说明：适用的详述其处理路径，不适用的注明 `N/A：[一句话说明不涉及的理由]`。

---

### 图后文字说明（必须）

- **位置**：每张 `sequenceDiagram` 代码块**紧下方**（中间不得插入其他图表或无关大段内容），须有独立小节，标题建议统一为「**协作者与过程说明**」或与文档模板一致的等价标题（如「协作过程」「工作流程说明」）。
- **禁止**：仅用一句话概括、只列 participant 名单、或与图中消息逐条机械重复而无解释——均视为**不满足**本要求。
- **必须写清的内容**（按实际流程展开，可分段编号）：
  1. **触发与入口**：谁、在什么条件下发起交互；对应图中的首条或首批消息。
  2. **协作链与职责**：沿调用方向说明每一跳「谁调用谁、为了什么、输入/输出语义」；与 participant 分层或模块边界的关系。
  3. **工作过程与数据流**：中间状态如何变化（内存/缓存/持久化/网络等若图中涉及则须说明）；关键业务决策在何处做出。
  4. **分支与异常**：对图中每个 `alt/else`（及重要的 `opt/loop`）分别说明：进入条件、各分支的差异行为、最终对用户或调用方的可见结果。
  5. **结束条件**：正常结束与各类异常结束分别是什么；是否与流程图或其他设计章节一致。
- **粒度**：文字说明应达到**未读图也能理解主干协作与分支差异**的程度；AISDD 产出的 `key-func-design/KD_*_*.md`、`key-diagram-epic.md`、`features/*/key-diagram.md`、`features/*/l2_design/*.md` 等凡含时序图者，**一律遵守**本条。

---

## 五、流程图要求

- **完整性**：覆盖从触发条件到所有可能结束状态的完整逻辑路径，不得遗漏关键分支。
- **异常**：覆盖正常 + 异常分支。
- **一致性**：Feature 级流程图与时序图一一对应。

---

## 六、职责说明

- 类图后必须紧跟**关键类职责说明**表，按分层或功能分组组织。
- 每个类的职责用简洁一句话描述核心责任。
