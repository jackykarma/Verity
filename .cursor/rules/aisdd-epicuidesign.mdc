---
description: "当用户输入 /aisdd.epicuidesign 时触发：解析设计稿（交互稿/视觉稿），提取并结构化交互逻辑与视觉规范，产出可验证的 ux-design.md"
alwaysApply: false
---

# /aisdd.epicuidesign — EPIC 设计稿解析

用户输入 `/aisdd.epicuidesign`（可附 EPIC 标识、解析侧重 `交互`/`视觉`、Figma 链接）时触发：从设计素材中**提取并结构化**交互逻辑与视觉规范，产出 EPIC 根 `ux-design.md`（解析而非创作；无素材时进入兜底模式）。

**唯一事实源**：输入源优先级（design-notes > design-tokens > Pencil > 图片）、质量分级与置信度标注、解析步骤、AI 视觉理解验证图与边界守护见 `.cursor/commands/aisdd.epicuidesign.md`，执行前**必须完整阅读**该命令文件；模板见 `.specify/templates/ux-design-template.md`。

速记红线：须在所有 Feature spec 完成后运行；对 `epic.md` / 各 `spec.md` 只读，遗漏场景登记到「遗漏与待确认」并引导 `/aisdd.cr` 或 `/aisdd.clarify`，禁止反写；行为契约/量化指标/范围归 spec，写 ux 前按 block_ask 拦截（`.cursor/rules/aisdd-document-boundaries.mdc` Part B）；每条提取须标来源与置信度。
