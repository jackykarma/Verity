---
description: "当用户输入 /aisdd.techspec 时触发：产出 EPIC 根目录 tech-spec.md"
alwaysApply: false
---

# /aisdd.techspec — EPIC 技术规格书

用户输入 `/aisdd.techspec`（可附 EPIC 标识，如 `EPIC-001`）时触发：在 EPIC 根目录产出**唯一**技术规约事实源 `tech-spec.md`。

**唯一事实源**：完整前置条件、执行步骤、章节结构与行为约束见 `.cursor/commands/aisdd.techspec.md`，执行前**必须完整阅读**该命令文件；模板见 `.specify/templates/tech-spec-template.md`。

速记红线：所有 Feature `spec.md` 完成后才可运行；技术规约仅存在于 EPIC 根 `tech-spec.md`；不写详细设计（架构图/类图/时序/接口签名/表字段 → `/aisdd.epicdesign`）；对 `spec.md` 只读，禁止反写。
