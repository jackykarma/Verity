---
description: "当用户输入 /aisdd.techspec 时触发：产出 EPIC 根目录 tech-spec.md（合并 epic-plan 与各 Feature plan）"
alwaysApply: false
---

# /aisdd.techspec — EPIC 技术规格书

## 触发

用户输入 `/aisdd.techspec`，可附带 EPIC 标识（如 `EPIC-001`）。

## 目的

产出 EPIC 根目录下的 **`tech-spec.md`**，作为本 EPIC **唯一**的技术规约事实源。

## 前置条件

1. `epic.md` 已存在
2. **所有 Feature 的 `spec.md` 已完成**
3. 可与 `/aisdd.epicuidesign` 并行

## 输入（必须阅读）

1. **`epic.md`**
2. **所有 Feature 的 `spec.md`**
3. **`.specify/templates/tech-spec-template.md`**
4. **`.specify/memory/constitution.md`**
5. **现有工程代码**
6. **`ux-design.md`**（若存在）
7. **`research/`**（若存在，只读；禁止回写）

## 输出

`tech-spec.md`：

| 部分 | 内容 |
|------|------|
| 前置检查 + 变更记录 | 版本治理 |
| 第一部分 | EPIC 公共约束、跨 Feature 边界、运行时、数据总约束、共享能力、编写顺序 |
| 第二部分 | 每个 Feature 四小节：摘要、增量约束、能力边界、数据/NFR/安全 |

**不在 tech-spec**：NFR 量化 → `nfr.md`；接口/契约 → `interface-design.md`；详细设计 → `/aisdd.epicdesign`。

## 行为约束

- 不得创建 `epic-plan.md` 或 Feature 级 `plan.md`
- 模板结构保护；与 epic.md 双向对齐
- 变更记录填写版本

## 完成后

1. 评审 tech-spec.md
2. 推荐 `/aisdd.challenge plan`
3. `/aisdd.epicdesign`
