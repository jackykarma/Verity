---
description: "当用户输入 /speckit.verify 时触发：执行实现↔设计一致性验证"
alwaysApply: false
---

# /speckit.verify — 实现↔设计一致性验证

## 触发

用户输入 `/speckit.verify`，可附带 Feature ID 或 Story ID 限定验证范围。

## 目的

验证代码实现与设计方案的一致性，产出验证报告，作为交付前的最终质量关卡。

## 前置条件

1. 目标 Feature 的 `tasks.md` 中所有 Task 已完成（`- [x]`）
2. 代码已提交

## 输入（必须阅读）

1. **本 Feature 的 `tasks.md`**：所有 Task 及其验证清单
2. **`epic-design.md`**：架构、类图、时序图、技术风险、Story 拆解
3. **`key-diagram.md`**：全景类图、时序图
4. **本 Feature 的 `story_detail_design.md`**：L2 详细设计
5. **本 Feature 的 `plan.md`**：技术规约、接口契约、NFR 预算
6. **本 Feature 的 `spec.md`**：FR/NFR/AC
7. **实际代码**：已实现的代码文件

## 验证维度

### 1. 架构一致性

- 实际代码的分层与模块边界是否与 `epic-design.md` §五一致
- 依赖方向是否正确（上层依赖下层，无反向依赖）
- 跨层约束是否遵守

### 2. 类/接口一致性

- `key-diagram.md` 中定义的类是否在代码中存在
- 方法签名是否与类图一致（参数、返回值）
- 职责划分是否与设计一致

### 3. 流程/时序一致性

- 关键流程的调用链是否与时序图一致
- 异常分支是否已实现
- 错误处理是否与设计一致

### 4. 接口契约一致性

- 对外接口是否与 plan.md §五 契约一致
- 错误码/异常是否按设计实现
- 版本兼容性是否满足

### 5. NFR 达标检查

- 性能/功耗/内存指标是否在预算范围内（对照 spec.md NFR）
- 安全/隐私要求是否满足

### 6. FR/AC 覆盖

- 每条 FR 是否有对应实现
- 每条 AC 是否可验证通过
- spec.md 需求追溯表中的 Story→Task 链路是否完整

## 输出

验证报告使用 `.specify/templates/verify-report-template.md` 模板，写入 EPIC 目录下 `verify-report.md`。

报告包含 9 个章节（验证总览、架构一致性、类/接口一致性、流程/时序一致性、接口契约一致性、NFR 达标检查、FR/AC 覆盖、偏离项汇总、验证结论），每个维度用表格列出检查项和结论。

### 验证结论

- **通过**：所有维度均通过，可交付
- **有条件通过**：存在 Low/Med 偏离，列出遗留项及跟进计划
- **不通过**：存在 High 偏离，须修复后重新验证

## 行为约束

- **基于事实**：验证必须基于实际代码分析，不得假设
- **偏离项必须有证据**：指出具体文件、行号、与设计文档的差异
- **不自动修复**：验证只报告，不擅自修改代码；修复由用户决定

## 完成后

提示用户：
1. 验证报告结论
2. 若不通过：列出修复建议，用户决定是否修复或走 CR
3. 若通过：可更新 `gate-log.md` 的 Verify Pass 记录
4. 所有 Feature 验证通过 → EPIC 可合并主分支
