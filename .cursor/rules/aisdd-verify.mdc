---
description: "当用户输入 /aisdd.verify 时触发：执行实现↔设计一致性验证"
alwaysApply: false
---

# /aisdd.verify — 实现↔设计一致性验证

## 触发

用户输入 `/aisdd.verify`，可附带 Feature ID 或 Story ID 限定验证范围。

## 目的

验证代码实现与设计方案的一致性，产出验证报告，作为交付前的最终质量关卡。

## 前置条件

1. 目标 Feature 的 `tasks.md` 中所有 Task 已完成（`- [x]`）
2. 代码已提交

## 输入（必须阅读）

1. **本 Feature 的 `tasks.md`**：所有 Task 及其验证清单
2. **`epic-design.md`**：架构、§7.1 KD 清单与依赖、类图索引、技术风险、Story 拆解
3. **`key-func-design/KD_*_*.md`**（若存在）：各 KD 核心方案与 §7.1 一致性
4. **`nfr.md`**（若存在）：技术评估量化结论
5. **`key-func-design/KD_*_*.md`**（若存在）：关键设计、核心类图与核心调用链时序
6. **本 Feature 的 `l2_design/ST-xxx_*.md`**：复杂/高风险 Story 的 L2 详细设计（若有）；简单 Story 对照 `tasks.md` 设计引用与 DoD
7. **本 Feature 的 `plan.md`**：技术规约、接口契约、NFR 预算
8. **本 Feature 的 `spec.md`**：FR/NFR/AC
9. **实际代码**：已实现的代码文件

## 验证维度

### 1. 架构一致性

- 实际代码的分层与模块边界是否与 `epic-design.md` §五一致
- 依赖方向是否正确（上层依赖下层，无反向依赖）
- 跨层约束是否遵守

### 2. 类/接口一致性

- `key-func-design/KD_*_*.md` 与各 Feature `l2_design/ST-xxx_*.md` 中定义的核心类/接口是否在代码中存在；简单 Story 无独立 L2 时，对照 `tasks.md` 的设计引用与 DoD
- 方法签名是否与类图一致（参数、返回值）
- 职责划分是否与设计一致

### 3. 流程/时序一致性

- 关键流程的调用链是否与时序图一致
- 异常分支是否已实现
- 错误处理是否与设计一致
- **时序图图后说明**：每张关键时序图下方是否存在**详细**文字说明（协作链、工作过程、`alt/else` 分支语义、结束条件）；若缺失或仅有概括性一句话，视为设计文档不合格项（在验证报告中记录）
- （若存在 `key-func-design/KD_*_*.md`）`epic-design.md` §7.1 与各 KD 文首「依赖的其他 KD」是否一致，依赖是否为 DAG；各 KD **核心方案**是否覆盖技术点与**相关技术链路**、链上**如何达成**是否可与代码、KD 内流程图和核心时序图交叉核对；**核心方案**与对应流程图、核心时序图是否一致

### 4. 接口契约一致性

- 对外接口是否与 plan.md §四 契约一致
- 错误码/异常是否按设计实现
- 版本兼容性是否满足

### 5. NFR 达标检查

- 性能/功耗/内存/包体等指标是否在预算范围内（对照 `spec.md` NFR 与 EPIC 根目录 **`nfr.md`** 实测与结论，若存在）
- 安全/隐私要求是否满足

### 6. FR/AC 覆盖

- 每条 FR 是否有对应实现
- 每条 AC 是否可验证通过
- `tasks.md` 中 FR/NFR → Story → Task 追溯矩阵是否完整

## 输出

验证报告使用 `.specify/templates/verify-report-template.md` 模板，写入 EPIC 目录下 `verify-report.md`。

报告包含 9 个章节（验证总览、架构一致性、类/接口一致性、流程/时序一致性、接口契约一致性、NFR 达标检查、FR/AC 覆盖、偏离项汇总、验证结论），每个维度用表格列出检查项和结论。

### 验证结论

- **通过**：所有维度均通过，可交付
- **有条件通过**：存在 Low/Med 偏离，列出遗留项及跟进计划
- **不通过**：存在 High 偏离，须修复后重新验证

## 行为约束

- **基于事实**：验证必须基于实际代码分析，不得假设
- **偏离项必须有证据**：指出具体文件、行号、与设计文档的差异
- **不自动修复**：验证只报告，不擅自修改代码；修复由用户决定

## 完成后

提示用户：
1. 验证报告结论
2. 若不通过：列出修复建议，用户决定是否修复或走 CR
3. 若通过：可更新 `gate-log.md` 的 Verify Pass 记录
4. 所有 Feature 验证通过 → EPIC 可合并主分支
