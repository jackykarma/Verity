---
description: "当用户输入 /aisdd.featuretasks 时触发：产出某个 Feature 的 tasks.md（Story → Task 落地任务清单）；支持全量或按 ST-xxx 增量生成"
alwaysApply: false
---

# /aisdd.featuretasks — Task 拆解

用户输入 `/aisdd.featuretasks`（无参数/`all` 全量重写、`ST-xxx` 增量合并、`-h` 帮助）时触发：基于 `epic-design.md` §十二 Story 拆解、EPIC 根 `tech-spec.md` 与 `spec.md`，产出/更新当前 Feature 的 `tasks.md`（内置 FR/NFR → Story → Task 追溯矩阵）。

**唯一事实源**：范围参数、前置条件、任务生成与增量合并规则见 `.cursor/commands/aisdd.featuretasks.md`，执行前**必须完整阅读**该命令文件；Task 行格式与必含子项（依赖/设计引用/步骤/验证/产物）见 `.specify/templates/tasks-template.md`。

速记红线：Story 只来自 §十二，不得擅自发明；每个 Task 必须有设计引用与 `[ST-xxx]` 绑定；增量模式不得删除/覆盖非目标 Story 的 Task 与 `- [x]` 进度；不得反向修改已冻结的 `spec.md` / `tech-spec.md`。
