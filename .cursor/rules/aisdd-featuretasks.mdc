---
description: "当用户输入 /aisdd.featuretasks 时触发：产出某个 Feature 的 tasks.md（Story → Task 落地任务清单）；支持全量或按 ST-xxx 增量生成"
alwaysApply: false
---

# /aisdd.featuretasks — Task 拆解

## 触发

用户输入 `/aisdd.featuretasks`。作用于当前 Feature。

## 范围参数

| 参数 | 行为 |
|------|------|
| 无参数 / `all` | 为本 Feature **全部** Story 生成 `tasks.md`（整份重写） |
| `ST-xxx`（可多个） | 仅为指定 Story **增量**生成/更新 Task（合并进已有 `tasks.md`） |
| `-h` | 输出帮助，不读写文件 |

## 目的

基于 EPIC 软件设计说明书的 Story 拆解（§十二）、`tech-spec.md`（本 Feature 章节）的技术规约与 spec.md 的 FR/NFR，将 Story 拆解为可执行的 Task 清单，产出/更新当前 Feature 目录下的 `tasks.md`，并在 `tasks.md` 内维护 FR/NFR → Story → Task 追溯矩阵。

## 前置条件

### 全量（无参数 / `all`）

1. 本 Feature 的 `spec.md` 和 `tech-spec.md` 已完成
2. `epic-design.md` §十二 Story 拆解已完成
3. 本 Feature 各 Story 的 L2 已就绪，或 `epic-design.md` §十三 已标注由 `tasks.md` 设计引用与 DoD 承接

### 增量（`ST-xxx`）

1. 指定 Story 已在 §十二 中定义且属于当前 Feature
2. 该 Story 的 L2 已就绪，或 §十三 已标注由 `tasks.md` DoD 承接
3. **不要求**其他 Story 的 L2 已完成

## 输入（必须阅读）

1. **`epic-design.md`**：Story 拆解（§十二 Story 列表、依赖关系、§十二.6 FR/NFR 覆盖矩阵）、关键设计（§七 KD，若有）、架构与边界章节（§一～§六）、L2 索引（§十三）
2. **本 Feature 的 `tech-spec.md`**：轻量技术规约、能力边界、数据/NFR/安全硬约束
3. **本 Feature 的 `l2_design/ST-xxx_*.md`**：目标 Story 的 L2 详细设计（若有）
4. **本 Feature 的 `spec.md`**：FR/NFR/AC（Task 验证须追溯）
5. **EPIC 根 `tech-spec.md`**（若存在）：EPIC 级技术约束
6. **已有 `tasks.md`**（增量模式必读）：保留非目标 Story 内容与 `- [x]` 进度
7. **`.specify/templates/tasks-template.md`**：模板结构

## 前置步骤：追溯矩阵与一致性核对

在产出 `tasks.md` **之前**，只做只读核对与 tasks 内追溯建模：

1. 从 `epic-design.md` §十二.6 覆盖矩阵提取 FR/NFR → Story 映射，并准备写入 `tasks.md` 的 FR/NFR → Story → Task 追溯矩阵（全量写完整表；增量只更新目标 Story 行）。
2. 核对 `tech-spec.md` §二～§四是否已被设计说明书承接；若发现 spec/tech-spec 与 design 矛盾或缺失，停止生成并建议走 `/aisdd.cr` 或更新指定设计章节。
3. **不得反向修改**已冻结的 `spec.md` 或 `tech-spec.md`；不得回填 spec 追溯表，不得追加 tech-spec 变更记录。

## 输出

`tasks.md`，包含以下内容：

### Task 行格式

```text
- [ ] T001 [P?] [ST-001] <带路径的任务标题>
```

- 复选框 `- [ ]`（完成后改 `- [x]`）
- 任务 ID（T001、T002…全局递增；**增量模式**新 ID 从已有最大编号 +1 起，不重编号其他 Story）
- `[P]` 标记可并行任务
- `[ST-xxx]` 绑定 Story ID（来自 EPIC 设计说明书 §十二 Story 拆解，不得擅自发明新 Story）
- 路径使用工程真实文件路径

### 每个 Task 必须包含

- **依赖**：T??? 或"无"
- **设计引用**：指向 `epic-design.md:§一～§六`、`key-func-design/KD_*_*.md`、复杂/高风险 Story 的 `l2_design/ST-xxx_<slug>.md:功能设计:类图/时序图`；简单 Story 无独立 L2 时引用 `epic-design.md:§十三` 的承接说明
- **步骤**：编号步骤列表
- **验证**：可执行的验证清单（含 NFR 阈值）
- **产物**：涉及的文件

### 阶段组织

- 阶段 0：准备（版本对齐、冻结设计输入）
- 阶段 1：环境搭建（目录/依赖/工具链）
- 阶段 2：核心基础（阻塞性前置，阻塞所有 Story）
- 阶段 3+：每个 Story 一个阶段（按依赖与优先级组织）
- 最终阶段：优化与跨领域关注点
- 依赖关系与执行顺序总结

### 增量合并规则（`ST-xxx` 模式）

- **保留**非目标 Story 的全部 Task 与 `- [x]` 状态
- **替换**目标 Story 的旧阶段与 Task
- **更新**追溯矩阵中目标 Story 相关行
- **重算**依赖关系 / 并行示例 / 落地策略（可标注尚未生成 Task 的 Story）
- **Tasks Version** 补丁 +0.0.1

### 全量规则（无参数 / `all`）

- 以模板整份生成；覆盖已有 `tasks.md` 时须警告将丢失勾选进度

### 测试任务

**测试任务可选**：仅当 spec 中明确要求，或用户指定采用 TDD 方式时，才生成测试任务。

## 行为约束

- **禁止改写技术决策**：Task 只拆解与执行既定设计，不得新增未决策的方案
- **设计引用必填**：每个 Task 必须有明确的设计引用
- **Story 绑定**：每个 Task 必须绑定 `[ST-xxx]`（来自 §十二）
- **追溯矩阵必填**：`tasks.md` 必须包含 FR/NFR → Story → Task 追溯矩阵
- **真实路径**：使用工程实际文件路径，不用示例路径
- **模板结构保护**：不得增删模板已定义的章节
- **独立可验证**：每个 Story 完成后可独立测试
- **执行主体**：SE/TL（或架构师）。开发者应将 tasks.md 视为只读执行清单；如需调整须提交变更提案
- **可直接执行**：每个任务描述需足够具体，确保无需额外上下文即可完成

## 完成后

提示用户：
1. 评审 tasks.md
2. 增量模式：可对下一 Story 继续 `/aisdd.featuretasks ST-xxx`；全部就绪后可 `/aisdd.featuretasks all` 做全量核对
3. **可直接** `/aisdd.implement`（`all` / `ST-xxx` / `Txxx`；必经下一步）
4. （可选）多 Feature 可 `/aisdd.analyze epic`；各 Feature 可 `/aisdd.analyze`——跳过不影响实现
