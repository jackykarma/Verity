---
description: "当用户输入 /aisdd.pipeline 时触发：端到端编排 AISDD 各阶段（epicspec → featurespec → techspec → epicdesign → featuretasks → implement），并在实现后进入构建/测试/验收→修复→复验闭环，直至全部完成"
alwaysApply: false
---

# /aisdd.pipeline — 端到端流水线编排

用户输入 `/aisdd.pipeline`（`EPIC-xxx` 目标、`--from`/`--until` 阶段范围、`--feature` 限定、`--auto`(默认)/`--interactive`、`--with-challenge`/`--with-ux`/`--with-research`、`--scope`、`--max-iters=N`、`--dry-run`、`-h` 帮助）时触发：作为**编排器**按现状自动推进各 AISDD 阶段，并在实现阶段后驱动「实现 → 验证（构建/测试/验收）→ 修复 → 复验」迭代闭环，直至达成完成定义（DoD）。

**唯一事实源**：阶段状态机、门禁规则、实现-验证-修复闭环、完成定义（DoD）、硬暂停点与红线见 `.cursor/commands/aisdd.pipeline.md`，执行前**必须完整阅读**该命令文件；各阶段产出规则仍以对应 `/aisdd.*` 命令文件为准，逐阶段执行前必须阅读对应命令文件。

速记红线：本命令**只编排不重写阶段逻辑**；**不得**反向改写已冻结的 `spec.md` / `tech-spec.md` / `epic-design.md`（变更走 `/aisdd.cr`）；产物达标判定以脚本输出与文件实况为准，不靠假设；Task 仅在真正完成并通过构建/测试后标 `[x]`；遇设计缺口、评审 BLOCK、迭代超 `--max-iters`、破坏性操作时**硬暂停**等待人工。
