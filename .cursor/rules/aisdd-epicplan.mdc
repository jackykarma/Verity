---
description: "当用户输入 /aisdd.epicplan 时触发：产出 EPIC 级技术规约 epic-plan.md"
alwaysApply: false
---

# /aisdd.epicplan — EPIC 技术规约

## 触发

用户输入 `/aisdd.epicplan`，可附带 EPIC 标识（如 `EPIC-001`）。

## 目的

产出 EPIC 根目录下的 `epic-plan.md`，定义所有 Feature 必须遵守的 EPIC 级技术约束与规约。

## 前置条件

1. `epic.md` 已存在
2. **所有 Feature 的 `spec.md` 已完成**（Spec Ready）
3. 可与 `/aisdd.epicuidesign` 并行执行

## 输入（必须阅读）

1. **`epic.md`**：背景、范围、约束、Feature 列表、跨 Feature 技术策略、EPIC-FR/NFR
2. **所有 Feature 的 `spec.md`**：FR/NFR/依赖/约束
3. **`.specify/templates/epic-plan-template.md`**：模板结构
4. **`.specify/memory/constitution.md`**：章程约束（尤其§三 技术栈、§二 演进式设计）
5. **现有工程代码**：实际技术栈、分层、模块结构
6. **`research/` 目录下的调研报告**（若存在，**参考性补充信息**）：`/aisdd.research --save` 的产出，含 API 限制、库评估、风险等技术背景——辅助了解但**不作为约束源或结论**，plan 仍须独立完整分析

## 输出

EPIC 根目录下的 `epic-plan.md`，覆盖模板全部章节：

| 章节 | 内容 |
|------|------|
| 一、EPIC 级公共约束 | 锁定语言、框架、构建、API 级别等 |
| 二、跨 Feature 边界与依赖规则 | 依赖方向、Feature 边界、跨层禁止 |
| 三、统一运行时约束 | 线程/协程、错误处理、日志、权限与安全原则 |
| 四、数据与存储总约束 | SoR、缓存、迁移原则 |
| 五、跨 Feature 共享能力 | 与 epic.md 对齐，确定 Owner 与执行顺序 |
| 六、Feature Plan 裁剪规则 | 各 Feature 用 Lite 还是 Standard |

**不在 epic-plan**：NFR 量化 → `nfr.md`；接口/契约 → `interface-design.md`（均由 `/aisdd.epicdesign` 产出）。

## 行为约束

- **基于现有代码**：技术栈、分层必须反映工程实际（constitution §一、§三）
- **模板结构保护**：不得增删章节（constitution §九）
- **与 epic.md 双向对齐**：§五 必须与 epic.md「跨 Feature 技术策略」一致；若有差异须提示用户同步
- 变更记录填写初始版本 v0.1.0

## 完成后

提示用户：
1. 评审 epic-plan.md
2. 后续各 Feature 按 §五 确定的执行顺序逐个运行 `/aisdd.featureplan`
3. NFR 与接口详细设计在 epicdesign 阶段由 `nfr.md`、`interface-design.md` 完成
