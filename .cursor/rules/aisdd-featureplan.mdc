---
description: "当用户输入 /aisdd.featureplan 时触发：产出某个 Feature 的 plan.md（Feature 技术规格）"
alwaysApply: false
---

# /aisdd.featureplan — Feature 技术规格

## 触发

用户输入 `/aisdd.featureplan`。作用于当前 Feature（由 `$env:SPECIFY_FEATURE` 或分支确定）。

## 目的

产出当前 Feature 目录下的 `plan.md`（Feature 技术规格）。plan.md 只回答"实现时必须遵守什么约束/契约/边界"，不回答"怎么设计、怎么协作、怎么拆解"。

**plan.md 输出的内容**：技术约束、外部依赖、接口契约、数据存储边界、合规性与安全约束。

**plan.md 不输出的内容**：架构图、类图、时序图、Story 拆解、源码目录设计、内部模块协作流程、L2 详细设计。这些由后续 `/aisdd.epicdesign` 在 EPIC 级统一产出。

## 前置条件

1. 本 Feature 的 `spec.md` 已完成（Spec Ready）
2. `epic-plan.md` 已存在（若为单 Feature EPIC 可省略，其内容合并到 plan.md）
3. 若 EPIC 级 `ux-design.md` 已产出（设计稿解析结果），作为可选输入

## 脚本

先运行 `.specify/scripts/powershell/setup-plan.ps1 -Json` 创建 plan.md 文件（从模板复制），解析 JSON 得到 `FEATURE_SPEC`、`IMPL_PLAN`、`EPIC_PLAN`、`UX_DESIGN` 等路径。

## 输入（必须阅读）

1. **本 Feature 的 `spec.md`**：FR/NFR/依赖/约束/边界场景——技术规格的主要输入
2. **`epic-plan.md`**（若存在）：EPIC 级技术约束（plan 不得违反）
3. **`ux-design.md`**（若已产出）：从设计稿解析出的信息架构、交互规则（页面流转图、逐屏交互规则）、视觉规范（色板、布局标注、组件清单）、设计稿索引；关注「遗漏与待确认」章节
4. **`research/` 目录下的代码调研快照**（若存在）：了解调研时代码现状——**不作为约束或决策依据**；**禁止**因 plan/CR 回写 `research/`；plan 仍须独立完整分析
5. **`.specify/templates/plan-template.md`**：模板结构
6. **`.specify/memory/constitution.md`**：章程约束（MUST/SHOULD）
7. **`.cursor/rules/specify-diagram-requirements.mdc`**：确保图表基于本工程实际架构
8. **现有工程代码**：实际架构、模块、技术栈

## 输出

`plan.md`，覆盖以下章节：

| 章节 | 内容 | 状态 |
|------|------|------|
| Plan 前置检查 | 验证 EPIC 级规约遵从、共享能力依赖、差距分析 | 填写 |
| §一 规约摘要 | Feature 目标、EPIC 约束来源、现有代码边界 | 填写 |
| §二 Feature 增量约束 | 相对 `epic-plan.md` 的增量（依赖/错误/日志/兼容性） | 填写 |
| §三 能力边界与外部依赖 | 对外能力承诺、依赖外部能力、调用约束 | 填写 |
| §四 数据、NFR 与安全硬约束 | 存储形态/SoR/生命周期/迁移/NFR/合规 | 填写 |
| 复杂度跟踪 | 突破最小改动原则时按 constitution 要求临时追加 | 按需 |

## 行为约束

- **演进式设计**：基于现有代码分析，不得从零设计理想方案（constitution §一、§二）
- **差距分析**：需求映射→差距分析→演进设计（constitution §四）
- **不得违反 epic-plan**：技术栈、分层、线程模型等必须与 EPIC 级约束一致
- **共享能力复用**：若 Owner 是其他 Feature，必须复用而非另起炉灶
- **模板结构保护**：不得增删章节
- **图表规范**：所有技术图表必须使用 Mermaid 格式，遵循 `.cursor/rules/mermaid-style-guide.mdc`
- **plan 定位**：plan.md 是技术规格（约束/契约/边界），不包含系统设计内容；架构图、类图、时序图、Story 拆解、源码目录设计由 `/aisdd.epicdesign` 在 EPIC 级统一产出
- **执行主体**：SE/TL（或架构师）。开发者应将 plan.md 视为只读输入；如需变更须提交变更提案

## 完成后

提示用户：
1. 评审 plan.md
2. 所有 Feature 的 plan 完成后 → `/aisdd.epicdesign` 产出 EPIC 软件设计说明书（可选先 `/aisdd.challenge plan`）
