---
description: "当用户输入 /aisdd.epicspec 时触发：填充 EPIC 级 epic.md（EPIC 规格说明 + Feature 拆分）"
alwaysApply: false
---

# /aisdd.epicspec — EPIC 规格说明

用户输入 `/aisdd.epicspec` 并附带 EPIC 描述时触发：创建 EPIC 目录（默认新建 `epic/EPIC-###-*` 分支，可选 `-UseCurrentBranch`），按 `.specify/templates/epic-template.md` 填充 `epic.md`（EPIC 规格 + Feature 拆分）。

**唯一事实源**：执行步骤、分支开关、Feature 拆分要求与纯净度守护见 `.cursor/commands/aisdd.epicspec.md`，执行前**必须完整阅读**该命令文件。

速记红线：`epic.md` 是需求事实源，**禁止**技术实现细节（守护规则见 `.cursor/rules/aisdd-document-boundaries.mdc`）；Feature 从业务场景视角拆分，不按技术分层拆分；各 Feature 的 spec 由 `/aisdd.featurespec` 单独创建。
