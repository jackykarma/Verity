---
description: "当用户输入 /aisdd.epicspec 时触发：填充 EPIC 级 epic.md（EPIC 规格说明 + Feature 拆分）"
alwaysApply: false
---

# /aisdd.epicspec — EPIC 规格说明

## 触发

用户输入 `/aisdd.epicspec` 并附带 EPIC 描述（大需求/主题）。

## 目的

基于用户输入的需求描述，填充 EPIC 根目录下的 `epic.md`，完成需求分析、Feature 拆分与跨 Feature 策略定义。

## 与其他命令的关系

- `/aisdd.epicspec`：**EPIC 入口**——产出 `epic.md`（EPIC 规格 + Feature 拆分列表）
- `/aisdd.featurespec`：**Feature 入口**——创建 Feature 文档目录 + 产出 `spec.md`（Feature 级规格）

## 前置条件

1. `create-new-epic.ps1` 已运行，EPIC 目录与 Git 分支已创建
2. EPIC 目录下存在空的 `epic.md`（从 `.specify/templates/epic-template.md` 复制）

## 输入（必须阅读）

1. **`epic.md`（当前 EPIC）**：获取模板结构
2. **`.specify/templates/epic-template.md`**：模板参考
3. **`$ARGUMENTS`**：用户的 EPIC 描述（大需求/主题）
4. **现有工程代码**（可选但建议）：了解现有架构以辅助 Feature 拆分与能力识别

## 输出

填充后的 `epic.md`，覆盖模板中所有章节：

### 必填章节

| 章节 | 内容 |
|------|------|
| 背景、目标与价值 | 背景、目标、价值、产品/技术竞争力、可量化成功标准 |
| 范围 | In/Out Scope、上项范围（地区/渠道/设备/OS）、上项节奏 |
| 约束与假设 | 平台/版本/合规/组织约束、假设及不成立时的应对 |
| 依赖关系 | 上游依赖、下游影响、外部依赖故障模式 |
| Feature 拆分 | Feature 列表（含类型/价值/依赖/NFR 关注点/优先级）+ 每个 Feature 的边界说明 |
| 通用能力 | 跨 Feature Capability 识别、能力清单、Capability Feature 映射 |
| 跨 Feature 技术策略 | 共享能力识别（Owner/消费方）、Feature Plan 执行顺序、技术约束 |
| 整体 FR/NFR（EPIC Level） | EPIC-FR（端到端功能目标）、EPIC-NFR（性能/功耗/内存/安全/可观测性） |
| EPIC 验收 | 验收前置条件、端到端验收场景（E2E-xxx）、通过准则 |
| 变更记录 | 初始版本 v0.1.0 |

### 可选章节

| 章节 | 条件 |
|------|------|
| EPIC 级风险与里程碑 | 建议填写；高风险 EPIC 必填 |

## 行为约束

### Feature 拆分规则

- 每个 Feature **必须可独立交付**（Product / Platform-Capability）
- FR/NFR 属于 Feature（在 `spec.md` 中细化），不属于 EPIC；EPIC 只提供边界与方向
- Feature 列表仅为候选，后续逐个运行 `/aisdd.featurespec` 创建文档目录

### Capability Feature 强制规则

- **埋点/可观测性**：若 EPIC 存在埋点要求，必须单独创建 Capability Feature
- **动效/交互体验**：若存在大量特殊动效/交互规范，必须单独创建 Capability Feature
- **算法能力**：每一个算法能力/模型必须单独创建 Capability Feature

### 跨 Feature 技术策略规则

- 必须识别共享技术能力，明确 Owner Feature 与消费方 Feature
- 必须基于依赖关系确定 Feature Plan 执行顺序（Owner 先行）
- 技术约束为后续所有 Feature 的 plan 提供强制输入

### 通用约束

- **模板结构保护**：不得增删模板已定义的章节（constitution §九）
- **成功标准可量化**：必须包含具体指标，避免实现细节
- **EPIC-NFR 可量化**：每条 EPIC-NFR 必须有数值阈值
- **范围一致**：Feature 的 In/Out Scope 之和不得超出 EPIC Scope
- **聚焦"做什么"**：避免描述"如何实现"，面向业务相关方编写
- **合理推测**：结合上下文和行业标准填补信息空白，在"假设"中记录
- **限制澄清次数**：最多 3 个 `[需澄清]` 标记，仅用于对范围/安全/体验有重大影响的关键决策
- **状态更新**：填充完成后将 `epic.md` 头部状态改为「就绪（可拆分 Feature）」

## 完成后

提示用户：
1. 评审 epic.md 内容（尤其 Feature 拆分与跨 Feature 技术策略）
2. 逐个 Feature 运行 `/aisdd.featurespec <Feature 描述>` 创建 Feature 目录与 spec.md
3. 所有 Feature 的 spec.md 完成后 → 可进入 `/aisdd.epicplan` 或 `/aisdd.epicuidesign`
