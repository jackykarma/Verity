---
description: "AISDD 文档边界：spec 纯净度、spec↔tech-spec/epic-design、spec↔ux-design"
globs: specs/**/spec.md, specs/**/epic.md, specs/**/ux-design.md, specs/**/tech-spec.md, specs/**/epic-design.md
alwaysApply: false
---

# AISDD 文档边界说明

> **定位**：治理 AISDD 工作流中各事实源文档的职责划分、污染识别、守护机制与实操判断。**Part A** 覆盖 `spec.md` ↔ `tech-spec.md` / `epic-design.md`；**Part B** 覆盖 `spec.md` ↔ `ux-design.md`。二者互补不重叠。
>
> **相关**：`.specify/memory/constitution.md`、`.specify/templates/spec-template.md`、`change-request-template.md`、`docs/aisdd/workflow-overview.html`

---

## Part A — spec.md 与 tech-spec.md / epic-design.md

### A.1 事实源归属

| 文档 | 事实源角色 | 主要内容 | 禁触领域 |
|------|------------|----------|----------|
| `spec.md` | **需求事实源** | 范围（In/Out）、FR、NFR（量化指标）、AC、完整场景矩阵、依赖关系 | **任何技术实现细节** |
| `tech-spec.md` | **EPIC 唯一技术规约事实源** | 第一部分 EPIC 公共约束；第二部分各 Feature 增量约束、能力边界、数据/NFR/安全硬约束 | 详细设计（架构图、类图、时序、表字段、接口签名） |
| `epic-design.md` 及子文件 | **架构与详细设计事实源** | 0/1 层架构、KD、接口/数据库/埋点、Story 拆解、L2 | — |

**一句话**：`spec.md` = 做什么 + 可验收结果；`tech-spec.md` = 技术框架内约束与能力边界；`epic-design.md` = 具体怎么做。

### A.2 技术污染识别清单（spec.md 必拦截）

| 污染类别 | 识别特征 | 正确归属 |
|----------|----------|----------|
| **类名 / 接口名** | `*ViewModel` / `*Repository` / `*UseCase` / `*Manager` / `*Service` / `*DataSource` 等 | `tech-spec.md` Feature §三 / `epic-design.md` |
| **框架 / 库名** | Hilt / Room / Compose / Coroutines / Retrofit / WorkManager 等 | `tech-spec.md` 第一部分或 Feature §二 |
| **数据存储细节** | DAO / Entity / 表名 / 字段 / SQL / 索引 | `database-design.md` |
| **API / 接口细节** | URL 路径 / HTTP 方法 / DTO 字段 | `interface-design.md` |
| **代码结构** | 包路径 / 文件路径 / Gradle 模块 / 代码片段 | `epic-design.md` |
| **线程 / 并发原语** | `Dispatchers.*` / `viewModelScope` / `Mutex` 等 | `tech-spec.md` / `epic-design.md` |
| **设计模式实现** | 单例/观察者等代码层表述 | `epic-design.md` |
| **埋点字段** | 事件名 / 参数 key / SDK 名 | `analytics-tracking.md` |

### A.3 判断分界点

| # | 问题 | 行动 |
|---|------|------|
| 1 | 删掉这条后「做什么 + 可验收结果」仍完整？ | 是 → 删技术细节 |
| 2 | 能否改写为纯业务语言？ | 能 → 改写后留 spec |
| 3 | 改写后仍含框架/类名/表/SQL/API/代码/线程/埋点？ | 是 → 继续剥离 |
| 4 | 描述业务能力/规则/可量化指标？ | 是 → 合法 spec |
| 5 | 描述实现选型/类结构/接口契约？ | 是 → 归 tech-spec / epic-design |

| 维度 | 归属 | 示例 |
|------|------|------|
| What & 量化阈值 | `spec.md` | 「批量删除 5s 内完成」 |
| 约束与边界 | `tech-spec.md` | 「依赖 FEAT-010；并发上限 16 张」 |
| 详细实现 | `epic-design.md` 与子文件 | 「`GalleryBulkDeleteService` 调用 `PhotoDao.deleteByIds`」 |

### A.4 NFR 特殊处理

NFR 只写**结果性指标 + 业务触发条件**，不写实现方式。❌ `Dispatchers.IO` / WorkManager / Firebase Analytics → 归 tech-spec 或 epic-design / analytics-tracking。

### A.5 CR 时 spec 边界

| CR 类型 | 可改 spec | 禁触 |
|---------|-----------|------|
| **需求类** | FR / NFR / AC / 范围 / 场景矩阵 / 依赖 | 技术实现细节 |
| **技术方案类** | 🚫 默认不改；**唯一例外** NFR 指标数值/口径 | 实现方案、库、类名、接口等 |
| **混合类** | 须在 CR §3.1 **显式标注** spec 修改范围 | 同上 |

写入前执行纯净度自检；命中按 block_ask：（a）改写留 spec（b）移出 tech-spec/design（c）保留并说明（d）拆分。

### A.6 工作流守护

**写入侧**：`aisdd.featurespec` / `aisdd.epicspec` / `aisdd.clarify` / `aisdd.cr`；模板 `spec-template.md`、`epic-template.md`、`change-request-template.md`。

**读取侧（禁反写 spec）**：`aisdd.techspec`、`aisdd.epicuidesign`、`aisdd.epicdesign`、`aisdd.featuretasks`、`aisdd.implement`。

### A.7 速记

**spec** = What + When + 量化 + 业务约束。**tech-spec** = Constraints + Boundaries。**epic-design** = How + 详细设计。

**试金石**：删掉技术词汇后仍完整可验收。

---

## Part B — spec.md 与 ux-design.md

### B.1 事实源归属

| 文档 | 角色 | 主要内容 |
|------|------|----------|
| `spec.md` | **需求事实源** | 范围、FR/NFR、AC、边界与异常场景 |
| `ux-design.md` | **体验呈现事实源** | 信息架构、交互规则、视觉规范、动效、设计稿索引 |

**一句话**：`spec.md` = 系统必须做什么；`ux-design.md` = 用户如何感知与操作。

### B.2 三维度划分

| 维度 | 问题 | 归属 |
|------|------|------|
| 产品/业务 | 必须产生什么结果？ | `spec.md` |
| 交互 | 什么操作、什么交互形态？ | `ux-design.md` |
| 视觉 | 色值、间距、动效参数？ | `ux-design.md` |

**原则**：删掉交互与视觉后行为结果仍完整可验收 → 属 spec；否则 → ux-design。

### B.3 异常与状态（重叠地带）

| 层面 | spec | ux-design |
|------|------|-----------|
| 行为与验收 | 须提示错误、可重试、数据不丢 | — |
| 呈现 | — | 错误态插画/文案、Snackbar/Dialog、组件样式 |

**spec** = 必须处理到什么程度；**ux-design** = 用什么形态让用户感知并完成操作。

### B.4 实操判断清单

| 问题 | 写入 |
|------|------|
| 去掉后逻辑不完整？ | `spec.md` |
| 描述控件/手势/动画/布局？ | `ux-design.md` |
| 可写成「系统必须……」？ | `spec.md` FR |
| 依赖设计稿定稿？ | `ux-design.md` |
| 可量化指标？ | `spec.md` NFR |
| 某状态下界面呈现？ | `ux-design.md` |
| 何时进入/退出状态（业务语义）？ | `spec.md` |

### B.5 与技术方案合流

```text
spec.md（做什么）     ─┐
                        ├──→ tech-spec.md / epic-design.md ──→ tasks.md → 实现
ux-design.md（怎么呈现）─┘
```

技术决策（线程、存储、API、类结构）归 `tech-spec.md` / `epic-design.md`，不在 ux-design 展开。

### B.6 工作流守护

- **featurespec 写 spec**：检出交互/视觉/布局越界 → block_ask，不归 ux 的直接写入 spec。
- **epicuidesign 写 ux**：检出行为契约/量化指标/范围 → block_ask，不归 spec 的直接写入 ux。
- **结构化输入**：`design-notes.md` > `design-tokens.json` > Pencil > 图片；冲突记入 ux「输入源冲突」。
- **AI 理解验证图**：`design/ai-understanding/`，在 ux-design 引用。

### B.7 速记

**spec** = What + When + 量化。**ux-design** = How it looks + How to interact。
