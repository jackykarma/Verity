---
description: "当用户输入 /aisdd.featureuidesign 时触发：在本 Feature spec 完成后解析设计稿，产出 features/FEAT-xxx/ux-design.md"
alwaysApply: false
---

# /aisdd.featureuidesign — Feature 设计稿解析

用户输入 `/aisdd.featureuidesign`（可附 Feature 标识、解析侧重 `交互`/`视觉`、Figma 链接）时触发：从设计素材**提取并结构化**本 Feature 的交互与视觉规范，产出 `features/FEAT-xxx/ux-design.md`。

**唯一事实源**：在 `/aisdd.featurespec` 之后、按 Feature 运行；三种输入（本地图片 / Figma MCP / Pencil）；设计素材优先 `features/FEAT-xxx/design/`。详见 `.cursor/commands/aisdd.featureuidesign.md`；模板见 `.specify/templates/ux-design-template.md`。

速记红线：须本 Feature `spec.md` 已完成；对 spec/epic 只读；每条提取标来源与置信度；跨 Feature 全局壳层在「交界」节摘要，细节留在各 Feature ux-design。
