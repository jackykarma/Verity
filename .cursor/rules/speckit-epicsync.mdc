---
description: "当用户输入 /speckit.epicsync 时触发：同步 epic.md Feature Registry 状态"
alwaysApply: false
---

# /speckit.epicsync — 同步 Feature Registry

## 触发

用户输入 `/speckit.epicsync`，可附带备注信息。

## 目的

将当前 Feature 的状态（版本号、进度）同步到 `epic.md` 的 Feature Registry 表格中。

## 执行方式

运行 PowerShell 脚本：

```powershell
.specify/scripts/powershell/sync-epic-overview.ps1 [-Notes "备注"] [-OverwriteNotes]
```

## 脚本行为

1. 从当前 Feature 的 `spec.md` 解析 EPIC ID 和 Feature 名称
2. 从 `spec.md`、`plan.md`、`tasks.md` 提取各自的 Version
3. 推断 Feature 状态（Spec Ready / Plan Ready / Tasks Ready）
4. 定位 `epic.md` 中 `<!-- BEGIN_FEATURE_REGISTRY -->` 和 `<!-- END_FEATURE_REGISTRY -->` 标记之间的区域
5. 更新或新增该 Feature 的行

## 前置条件

1. 当前 Feature 的 `spec.md` 必须存在且包含 `**Epic**：EPIC-xxx - ...` 行
2. `epic.md` 必须存在且包含 Feature Registry 标记（`<!-- BEGIN_FEATURE_REGISTRY -->`、`<!-- END_FEATURE_REGISTRY -->`）
3. 设置了 `$env:SPECIFY_FEATURE` 或处于正确的分支上

## 调用时机

- 每个阶段完成后建议运行一次：
  - Spec Ready 后
  - Plan Ready 后
  - Tasks Ready 后
  - Design Ready 后
  - Verify Pass 后

## 行为约束

- 脚本只更新 Registry 标记之间的内容，不修改 epic.md 其他部分
- 若 Feature 已存在，更新该行；若不存在，追加新行
- 备注列保留已有内容（除非使用 `-OverwriteNotes`）
