---
description: "当用户输入 /aisdd.implement 时触发：分阶段执行 tasks.md 中的任务，实现代码；支持全量、按 ST-xxx 或 Txxx 指定范围"
alwaysApply: false
---

# /aisdd.implement — 代码实现

用户输入 `/aisdd.implement`（无参数/`all` 全量、`ST-xxx` Story 范围、`Txxx` Task 范围、`-h` 帮助）时触发：按当前 Feature 的 `tasks.md` 分阶段实现代码。

**唯一事实源**：范围参数、依赖闭包规则、必读输入清单与执行流程见 `.cursor/commands/aisdd.implement.md`，执行前**必须完整阅读**该命令文件。

速记红线：必读 EPIC 根 `tech-spec.md`、`epic-design.md`、涉及 Story 的 `l2_design/ST-xxx_*.md`（若有）与 `spec.md`；**不得**擅自改写已冻结的 spec/tech-spec/design（变更走 `/aisdd.cr`）；完成的 Task 须在 `tasks.md` 标记 `[x]`；`/aisdd.analyze` 可选、非阻塞。
