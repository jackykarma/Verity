---
description: "当用户输入 /aisdd.implement 时触发：分阶段执行 tasks.md 中的任务，实现代码"
alwaysApply: false
---

# /aisdd.implement — 代码实现

## 触发

用户输入 `/aisdd.implement`，可附带 Task ID（如 `T101`）或 Story ID（如 `ST-001`）。

## 目的

按 `tasks.md` 中的 Task 分阶段实现代码，确保实现与设计一致。

## 前置条件

1. 本 Feature 的 `tasks.md` 已完成（Implement Ready）
2. 检查清单状态（若 `checklists/` 目录存在）：所有检查清单项已完成，否则默认停止（除非用户显式指定"继续/强制/force"）

## 输入（必须阅读）

1. **本 Feature 的 `tasks.md`**：完整任务列表和执行计划
2. **本 Feature 的 `plan.md`**：技术规约与实现约束
3. **`epic-design.md`**：架构设计、全景类图、关键时序、Story 拆解——架构事实源
4. **各 Feature 的 `story_detail_design.md`**：Story L2 详细设计（类图、时序图、触发条件）——详细设计事实源
5. **本 Feature 的 `spec.md`**：FR/NFR 与验收边界
6. **`epic-plan.md`**（若存在）：EPIC 级技术约束
7. **现有工程代码**：被修改的文件及其上下文

## 执行流程

1. **解析任务结构**：提取任务阶段（搭建/测试/核心开发/集成/优化）、依赖关系、设计引用
2. **分阶段执行**：完成一个阶段后再进入下一阶段
   - **遵循依赖关系**：顺序任务按序执行，并行任务 `[P]` 可同时执行
   - **引用设计**：实现每个 Task 时，先读取其设计引用指向的 `epic-design.md` 章节或 `story_detail_design.md` 对应 ST-xxx，确保实现与设计一致
   - **基于文件的协调**：影响同一文件的任务必须顺序执行
3. **验证检查点**：每个阶段完成后执行验证项
4. **标记完成**：任务完成后在 tasks.md 中将对应任务标记为 `[x]`
5. **进度跟踪**：每个任务完成后汇报进度；非并行任务失败则终止；并行任务失败则继续执行成功的任务并上报失败

## 执行顺序规则

- **搭建优先**：初始化项目结构、依赖项、配置项
- **先测后码**（若适用）：先完成测试任务
- **核心开发**：实现模型、服务、接口端点
- **集成工作**：数据库连接、中间件、日志
- **优化与验证**：性能优化、文档完善

## 行为约束（不可越权）

- **不得擅自改写设计**：实现必须与 `epic-design.md` 的架构图/类图/时序图和 `plan.md` 的技术规约一致；允许在细节上做必要调整但不得偏离整体设计方向
- **不得擅自改写 FR/NFR/AC**：`spec.md` 中的需求是实现的验收标准
- **遵循 EPIC 技术约束**：分层/线程/错误处理/命名等必须与 epic-plan.md 一致
- **演进式实现**：基于现有代码扩展，不得大规模重写（constitution §二）
- **最小改动原则**：能用 3 行代码解决的不要写 300 行（constitution §五）
- **发现设计缺口时**：停止 Implement，提交变更提案（建议使用 `change-request-template.md` 作为 CR 模板），由 SE/TL 先更新设计文档再继续

## 完成后

提示用户：
1. 输出最终状态，汇总已完成的工作
2. 检查已实现功能是否匹配 `spec.md` 的 FR/NFR/AC
3. 运行 `/aisdd.verify` 进行独立验证 → `/aisdd.gate implement-done` 通过完成关卡
