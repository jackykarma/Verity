---
description: "当用户输入 /speckit.backfill 时触发：epic-design Story 拆解完成后，回填 plan.md 和 spec.md"
alwaysApply: false
---

# /speckit.backfill — Story 拆解后回填

## 触发

用户输入 `/speckit.backfill`，可附带 Feature ID 限定范围（默认回填所有 Feature）。

## 目的

在 `epic-design.md` §十二 Story 拆解完成（Gate 5 通过）后，将 Story 信息回填到各 Feature 的 `plan.md` 和 `spec.md` 中，确保上下游产物一致。

## 前置条件

1. `epic-design.md` §十二 Story 拆解已完成且 Gate 5 已通过
2. 各 Feature 的 `plan.md` 初版已存在
3. 各 Feature 的 `spec.md` 已存在

## 输入（必须阅读）

1. **`epic-design.md` §十二**：Story 列表（按 Feature 分组）、Story ID/标题/类型/覆盖 FR-NFR
2. **各 Feature 的 `plan.md`**：当前 Story 索引表（应为空或占位）
3. **各 Feature 的 `spec.md`**：当前需求追溯表（应为空或占位）

## 回填操作（3 项）

### 1. 回填 plan.md「Story 索引表」

在每个 Feature 的 `plan.md` 末尾的「Story 索引表」中填入本 Feature 的 Story：

| Story ID | 标题 | 类型 | 设计说明书中的位置 |
|----------|------|------|---------------------|
| ST-xxx | [从 §十二 取] | [类型] | 设计说明书 §十二 - ST-xxx；L2 见 story_detail_design.md:ST-xxx |

### 2. 回填 spec.md「需求追溯表」

在每个 Feature 的 `spec.md` 末尾的「需求追溯」中填入 FR/NFR → Story 的映射：

| FR/NFR ID | 计划覆盖的 Story ID（Plan） | 任务覆盖（Tasks） | 备注 |
|---|---|---|---|
| FR-001 | ST-xxx | （tasks.md 产出后补填） | |

数据来源：`epic-design.md` §十二.6 覆盖矩阵。

### 3. 回填 plan.md「§一 一致性互校」

在每个 Feature 的 `plan.md` §一 中，对照 `epic-design.md` 的 §四（零层架构）和 §五（一层架构），逐行填写互校结果，确认无矛盾。

## 行为约束

- **精准更新**：仅修改上述 3 个章节/表格，不得修改 plan.md 和 spec.md 的其他章节（constitution §十）
- **数据一致**：所有回填数据必须与 `epic-design.md` §十二 的内容完全一致
- **版本更新**：回填完成后，更新 plan.md 的变更记录表（版本号 +0.0.1）

## 完成后

提示用户：
1. 回填已完成，列出已更新的文件清单
2. 建议运行 `/speckit.epicsync` 更新 Feature Registry
3. 后续逐个 Feature 运行 `/speckit.tasks` 产出任务清单
