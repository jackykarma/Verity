---
description: "当用户输入 /aisdd.cr 时触发：创建 CR、自动影响分析、生成下游更新清单并分步执行更新"
alwaysApply: false
---

# /aisdd.cr — 变更请求（CR）

用户输入 `/aisdd.cr`（无参数交互式、`analyze` 仅分析、`apply CR-xxx` 执行已有 CR、`-h` 帮助）时触发：为中途变更提供影响分析 → 生成 CR 文件 → 分步更新下游产物的端到端流程。

**唯一事实源**：模式参数、影响分析步骤、各变更类型的更新链路与 spec 纯净度红线见 `.cursor/commands/aisdd.cr.md`，执行前**必须完整阅读**该命令文件；CR 模板见 `.specify/templates/change-request-template.md`。

速记红线：技术方案类 CR 默认不动 `spec.md`（唯一例外：NFR 指标数值/口径）；`features/*/research/` 永不回写；精准更新、分步确认、每份文档更新 Version 与变更记录；变更影响超过 3 个 Feature 或需重构架构时建议新建 EPIC。
