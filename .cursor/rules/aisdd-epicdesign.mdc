---
description: "当用户输入 /aisdd.epicdesign 时触发：产出 EPIC 软件设计说明书（通过参数控制本次输出的章节范围，大章节输出到子文件并引用）"
alwaysApply: false
---

# /aisdd.epicdesign — EPIC 软件设计说明书

## 触发

用户输入 `/aisdd.epicdesign`，可附带章节范围参数控制本次输出内容。完整参数与步骤见 `.cursor/commands/aisdd.epicdesign.md`。

## 目的

在 EPIC 根目录产出 EPIC 软件设计说明书及配套子文件，作为面向人类评审与后续 Task 拆解、Implement 阶段 AI 编码引用的设计方案文档。与各 Feature 的 `plan.md`（技术规约）共同约束 tasks.md 与代码实现。

**Lite / Fast Track**：§一～§二 用索引表链到 `epic.md` / 各 `spec.md`；§十二 方法论见 `docs/aisdd/story-splitting-guide.md`（模板仅摘要）；P0 场景快照表可省略。

## 产出文件组（与 `epic-design-doc-template.md` 一致）

| 文件 / 目录 | 对应章节 | 内容 |
|-------------|----------|------|
| `epic-design.md` | §1～§6、§7 清单与引用、§8 摘要+`nfr.md`、§9～§11 摘要+三子文件链接、§十二 Story 拆解、§十三 L2 索引 | 设计总览；**§7.1** 为 KD 权威清单（含层级、前置 KD、路径）；**§8** 技术评估摘要链 **`nfr.md`**；**§9～§11** 不粘贴 interface/database/analytics 正文 |
| `key-func-design/KD_*_*.md` | §七（详细） | 按需生成，每关键设计一篇；须含**核心方案**、**关键类图（全量公共方法签名）**、**核心调用链时序图（穷举关键异常分支）**；按需可加方案架构图；无关键疑难点时 §七标注 N/A |
| `nfr.md` | §八（详细） | 按需生成；技术评估 8.1～8.7 全文（模板 `nfr-template.md`） |
| `interface-design.md` | §九（详细） | 按需生成；接口设计 9.1～9.3 全文 |
| `database-design.md` | §十（详细） | 按需生成；数据库设计 10.1～10.4 全文 |
| `analytics-tracking.md` | §十一（详细） | 按需生成；埋点方案 11.1～11.2 全文 |
| `features/FEAT-xxx/l2_design/ST-xxx_*.md` | §十三 | 复杂/高风险 Story 按需生成 L2 详细设计；简单 Story 可在 EPIC 根 **`epic-design.md` §十三.1/§十三.2** 标注由 `tasks.md` 设计引用与 DoD 承接 |

**禁止**：新 EPIC **不得**创建 EPIC 根目录 **`key-func-design.md`**（已废弃）。**不再**产出 **`key-diagram-epic.md`**、**`features/*/key-diagram.md`**（全景/子类图与完整时序已收敛在 **§七各 KD** 与 **§十三 L2**）。

## 子文件引用规则示例

**§七**（清单在 epic，正文在 KD 文件）：

```markdown
## 七、关键功能与疑难点/亮点设计

**摘要**：[2～3 句概述 KD 数量与分层]

### 7.1 关键设计清单
（表格含：编号、名称、层级/类型、前置 KD、路径 `./key-func-design/KD_001_xxx.md`）

### 7.2 详细设计引用
- [KD-001 名称](./key-func-design/KD_001_xxx.md)
```

**§八**（正文在 `nfr.md`）：

```markdown
## 八、技术评估（设计产出验证，必须量化）

**摘要**：[2～3 句]

→ 详见 [nfr.md](./nfr.md)
```

**§九～§十一**（正文分别在三个独立文件，`epic-design.md` 仅摘要 + 链接）：

```markdown
## 九、接口设计
**摘要**：[2～3 句]
→ 详见 [interface-design.md](./interface-design.md)

## 十、数据库设计
**摘要**：[2～3 句]
→ 详见 [database-design.md](./database-design.md)

## 十一、埋点技术方案
**摘要**：[2～3 句]
→ 详见 [analytics-tracking.md](./analytics-tracking.md)
```

**§十三**：各 Feature `l2_design/ST-xxx_*.md` 的**索引、关联 KD、前置依赖列、L2 状态与 EPIC 级依赖总览**写在 **`epic-design.md` §十三**（见 `epic-design-doc-template.md`）；复杂/高风险 Story 的 **L2 正文**在独立 Markdown 文件中，简单 Story 可标注由 `tasks.md` 设计引用与 DoD 承接。

## §七 / §八 / §九～§十一 / §十三 定位与粒度约束

设计说明书按 **`key → nfr →（接口/库表/埋点）→ story → l2`** 递进（与命令 `.cursor/commands/aisdd.epicdesign.md` 一致），**禁止**再引入独立的 key-diagram 阶段。

| 阶段 | 章节 | 定位 | 图表/产出粒度 |
|------|------|------|----------------|
| **`key`**（§七） | `key-func-design/KD_*.md` | **按需方案论证 + 编码级图表** — 仅关键疑难点/高风险设计生成 KD，KD 内须含全量公共方法签名的类图与时序全分支 | 单 KD 内完成「可评审、可互证」的类图/时序，**不**拆到单独 diagram 文件；无关键疑难点时 §七 N/A |
| **`nfr`**（§八） | `nfr.md` | **按需设计产出验证** — 涉及量化 NFR 时评估是否满足目标 | 实测与表格在 `nfr.md`，`epic-design.md` §八 仅摘要+链；不适用时 N/A |
| **契约与数据**（§九～§十一） | 三子文件 | 按需接口 / 库表与迁移 / 埋点规约 | 正文在适用子文件，epic 仅摘要+链；不适用时 N/A，不创建空文件 |
| **`l2`**（§十三） | `l2_design/ST-*.md` + epic §十三索引 | **Story 级落码设计** — 复杂/高风险 Story 按需切片并继承 KD | 复杂/高风险 Story 写完整类图/时序；简单 Story 可由 `tasks.md` 设计引用与 DoD 承接；索引在 epic §十三 |

- **§七 图表粒度（模板要求）**：每个 KD 内**关键类图为全量公共方法签名**，**核心调用链时序图穷举全异常分支**，与长文方案互证；跨 KD / 跨 Feature 流程在相关 KD 中互链说明。
- **§八 与 §七**：§八 基于 §七 方案做 NFR 量化验证，**不**承担「补全类图精度」职责（该精度由 §七 KD 保证）。
- **§十三 与 §七**：L2 在 §七 KD 与 §五/§六 架构上下文基础上**按 Story 范围切片**并补充落码细节；若 Story 涉及 KD 已定义的关键能力、核心类、接口契约、状态流转、异常策略或跨模块协作，L2 必须继承 KD 决策，只做 Story 级细化，不得脱离 KD 另起一套技术方案；复杂/高风险 Story 必做独立 L2，简单 Story 可在 §十三标注由 `tasks.md` 设计引用与 DoD 承接。

## 行为约束

- **基于真实代码**：类图/时序图须使用工程真实类名与方法名（`.cursor/rules/specify-diagram-requirements.mdc`）。
- **KD 核心方案（按需，若生成则必须）**：每个 `key-func-design/KD_*_*.md` 按 `.specify/templates/key-func-design-kd-template.md` 撰写；**全覆盖**技术点、**全链路**、**每环可落地**、与图互证。
- **KD 关键类图与时序（若生成 KD 则必须）**：每个 KD 须含**关键类图**（全量公共方法签名）与**核心调用链时序图**（穷举全异常分支）；**不再**使用 `key-diagram-epic.md` / `features/*/key-diagram.md` 作为补全层。
- **KD 图表归属（若生成 KD 则必须）**：每个 KD 的**关键类图**与**核心调用链时序图**（及按需的方案架构图）**直接绘制在该 KD 的 `KD_*_*.md` 内**；跨 KD / 跨 Feature 流程在相关 KD 中互链说明，**不再**单独产出流程图集。
- **KD 依赖（必须）**：`epic-design.md` §7.1 与各 KD 文首「依赖的其他 KD」**一致**；依赖关系须为 **DAG**，**禁止循环依赖**。
- **L2 继承 KD（适用时必须）**：生成 `features/*/l2_design/ST-*.md` 前须判定关联 KD，并在 `epic-design.md` §13.1 与 L2 文首「L2 依赖与引用」中记录；L2 的类图、时序图与功能设计须沿用关联 KD 的核心类/接口、依赖方向、调用职责、成功/失败语义和关键约束。若简单 Story 不生成独立 L2，须在 §13.1 写明由 `tasks.md` 设计引用与 DoD 承接。若需要改变 KD 决策，先更新 KD 或记录设计变更，禁止只在 L2 中另写冲突方案。
- **时序图图后说明（必须）**：凡含 `sequenceDiagram` 的 `key-func-design/KD_*_*.md`、`features/*/l2_design/*.md` 等，每张时序图**紧下方**须有详细协作者与过程说明（`.cursor/rules/specify-diagram-requirements.mdc` §四）。
- **Mermaid 样式**：遵循 `.cursor/rules/mermaid-style-guide.mdc`。
- **演进式设计**：不得提出整体重写（`.specify/memory/constitution.md`）。
- **子文件自包含**：各 **`KD_*_*.md`**、**`nfr.md`**、**`interface-design.md`**、**`database-design.md`**、**`analytics-tracking.md`**、各 **`features/*/l2_design/ST-*.md`** 须自包含可读；允许文内相对链接到其他 KD 或同目录下其他 ST 的 L2 文件。
- **章节骨架先行**：首次调用输出完整 **§1～§13** 骨架（与模板大章一致）；后续仅更新指定章节范围。

## 输入模板（须读取）

- `epic-design-doc-template.md`、`key-func-design-kd-template.md`、`nfr-template.md`、`epic-design-interface-template.md`、`epic-design-database-template.md`、`epic-design-analytics-tracking-template.md`、`story_detail_design_template.md`
- `specify-diagram-requirements.mdc`、`mermaid-style-guide.mdc`、`constitution.md`
- `research/` 目录下的代码调研快照（若存在）：辅助理解调研时代码现状；**不作为约束或设计依据**；**禁止**因 design/CR 回写 `research/`；设计仍须独立完整分析

## 完成后

按 `.cursor/commands/aisdd.epicdesign.md`「完成报告」提示下一步（key → nfr → story → l2 → featuretasks）。
