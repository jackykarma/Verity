---
description: "当用户输入 /aisdd.epicdesign 时触发：产出 EPIC 软件设计说明书（通过参数控制本次输出的章节范围，大章节输出到子文件并引用）"
alwaysApply: false
---

# /aisdd.epicdesign — EPIC 软件设计说明书

用户输入 `/aisdd.epicdesign`（无参数骨架+§1~§6 | `arch` | `key` | `nfr` | `story` | `l2 [范围]` | `all` | `-h`）时触发：在 EPIC 根产出 `epic-design.md` 及配套子文件（`key-func-design/KD_*.md`、`nfr.md`、`interface-design.md`、`database-design.md`、`analytics-tracking.md`、各 Feature `l2_design/ST-xxx_*.md`）。

**唯一事实源**：参数语义、产出文件组、阶段递进逻辑（key → nfr → story → l2）、子文件引用规则与 KD/L2 粒度约束见 `.cursor/commands/aisdd.epicdesign.md`，执行前**必须完整阅读**该命令文件；各产物模板见 `.specify/templates/epic-design-doc-template.md` 等（命令内列出）。

速记红线：前置 `tech-spec.md` 已产出（`HAS_TECH_SPEC`）；对 `spec.md` / `tech-spec.md` 只读，禁止反写；KD 内类图须含全量公共方法签名、时序穷举关键异常分支，KD 依赖须为 DAG；L2 必须继承关联 KD，不得另起方案；图表用真实类名（`specify-diagram-requirements.mdc`）与统一样式（`mermaid-style-guide.mdc`）；**不得**创建已废弃的根目录 `key-func-design.md`、`key-diagram-epic.md`、`features/*/key-diagram.md`；Story 拆解方法论见 `.cursor/rules/aisdd-story-splitting.mdc`。
