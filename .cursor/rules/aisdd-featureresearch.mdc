---
description: "当用户输入 /aisdd.featureresearch 时触发：Brownfield 时帮助设计者熟悉存量代码，并为 epicdesign 提供设计参考；纯 Greenfield 可跳过。"
alwaysApply: false
---

# /aisdd.featureresearch — Feature 代码调研

用户输入 `/aisdd.featureresearch`（可附 `FEAT-xxx`、子主题、`--skip`）时触发。**Brownfield** 时对照 spec/ux 调研存量代码，写入 `research/codebase-*.md`；**纯 Greenfield** 跳过，不写报告。

**调研目标**：① 帮助设计者熟悉当前相关代码；② 为后续 epicdesign 提供设计参考（只读事实，非约束源）。

**唯一事实源**：详见 `.cursor/commands/aisdd.featureresearch.md`；模板见 `.specify/templates/research-template.md`。

速记红线：只读、只记录事实；Greenfield 不跑；须含实现流程 + 工作原理；epicdesign 可读作设计参考但须独立决策；CR 不回写 `research/`。
